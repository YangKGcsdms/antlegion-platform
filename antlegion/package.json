{
  "name": "antlegion",
  "version": "0.1.0",
  "description": "Agent runtime for AntLegion Bus — fact-driven coordination for autonomous AI agents",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "antlegion": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/ws": "^8",
    "typescript": "^5.7",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=22"
  }
}
