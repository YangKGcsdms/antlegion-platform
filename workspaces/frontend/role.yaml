role: frontend-developer

claims:
  - bug.found

allowed_publish:
  - code.frontend.completed
  - bug.fixed

context_interests:
  - prd.published
  - api.contract.published
  - ui.prototype.published
  - code.backend.completed

max_concurrent_claims: 2
max_retries: 3
max_tool_rounds: 50
