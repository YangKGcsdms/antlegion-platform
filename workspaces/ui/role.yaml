role: ui-designer

# ── DDD 设计域 ──
# IN  (1/3): prd.published
# OUT (1/2): design.published

claims: []

allowed_publish:
  - design.published

publish_modes:
  - fact_type: design.published
    mode: broadcast

context_interests:
  - prd.published

max_concurrent_claims: 1
max_retries: 3
max_tool_rounds: 50
