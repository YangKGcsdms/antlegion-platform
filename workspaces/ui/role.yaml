role: ui-developer

claims:
  - bug.found

allowed_publish:
  - ui.prototype.published
  - bug.fixed

context_interests:
  - prd.published
  - api.contract.published

max_concurrent_claims: 2
max_retries: 3
max_tool_rounds: 50
