<div align="center">

[English](PROTOCOL.md) · 🌐 **简体中文**

</div>

# AntLegion 协议 —— v3.0

> 一个本原。一次写。一次读。其余一切皆由其推导。
>
> **为智能体共享世界状态的事实日志。**
>
> 由 **Carter.Yang** 设计，2026 年从第一性原理重新推导。

---

## 摘要

不共享进程的软件智能体 —— 不同机器、不同运行时、不同厂商 —— 没有任何办法就「什么是
真的」达成一致。每个都握着自己那份图景，而在它们之间搬运状态的是人。

本协议用一条**共享世界状态日志**取代这个中转。智能体把不可变、内容寻址的**事实**追加
到一条由总线赋予全序的只追加日志上。没有任何事实是发给某个人的。每个读者把同一条日志
**折叠**成同一个世界：X 现在是什么，它怎么来的、又引发了什么，该不该信，以及谁对它负责。

总线只做四件事 —— 定序、见证、签名、保存 —— 不持有任何逐事实状态。全部含义活在读者的
折叠里，而折叠是日志前缀的纯函数。因为序是全的、折叠是纯的，看到同一前缀的两个读者算出
同一个世界；而「一件活只做一次」是**这个序的一条定理**，不是一把锁：seq 最小的存活领取
胜出，每个读者不问任何人就算出同一个赢家。

**状态：草案。** 与 v2.0 不是 wire 兼容的（§C）。

---

## 本文状态

**版本 3.0。稳定性：草案。**

规范性章节是 §5（字段）、§7（操作）、§8（折叠）、§10（授权与强制）与 §11（存储）。一致性
真正活在 §8，因为总线是无状态的：一个把 §7 实现得完美、却把本章折叠得不一样的实现，在
任何有用的意义上都不能互操作。

关键词 MUST、MUST NOT、REQUIRED、SHALL、SHOULD、SHOULD NOT、RECOMMENDED、MAY、OPTIONAL
按 RFC 2119 与 RFC 8174 理解，且仅当它们以全大写出现时如此。

**本版本与 v2.0 不是 wire 兼容的。** 内容地址全部改变，若干折叠被收紧，总线开始拒绝它
过去接受的输入。完整变更清单与迁移立场见 §C。

---

## 1. 引言

### 1.1 问题

两个智能体在同一个世界里干活，除此之外什么都不共享。

一个是笔记本上的编码会话，一个是 CI 里的任务，一个是服务器上的常驻进程，一个是厂商托管
的智能体。它们不共享内存、进程树、调度器，也不共享框架。它们可能连语言和公司都不一样。

它们仍然需要就那个共同作用的世界的状态达成一致：哪个构建上线了、迁移跑没跑、客户说了
什么、事故归谁处理。今天在它们之间搬运这些状态的，是一个人，把内容从一个窗口粘到下一个。

在一棵进程树内部，这个问题不存在 —— 有子智能体、共享内存、有主管。在物理隔离的智能体
之间，没有更大的智能体可以退守。**只有人肉中转。**

### 1.2 为何现有做法不够

下面每一种机制都解决了一个相邻的问题，而它们在这个问题上失效的原因都是结构性的，不是
偶然的。

| 机制 | 它给了什么 | 为什么不是这个 |
|---|---|---|
| **消息队列** | 投递给消费者 | 消息是被**消费**掉的：投递之后状态就离开了介质、住进了消费者。后到的智能体无法重建世界。而且投递意味着收件人，收件人意味着命令。 |
| **共享数据库** | 一个当前值 | 你能拿到的只有当前值。它怎么来的、还有谁信它、有没有被质疑过，都不可恢复 —— 因为写是破坏性的。 |
| **编排器 / 工作流引擎** | 指派与排序 | 必须有人持有那份计划。那个人就是「更大的智能体」，而这正是跨组织时不存在的东西 —— 并且每个参与者都得接受它的权威。 |
| **事件总线（发布订阅）** | 广播 | 订阅者之间没有全序、没有持久前缀，于是两个订阅者可以正当地各执一词，而谁也证明不了自己对。 |
| **CRDT / 状态复制** | 无需协调即可收敛 | 收敛是拿「限制你能说什么」换来的 —— 直到每个操作都可交换为止。而分歧本身（「A 说 3，B 说 7」）恰恰是我们需要表达的东西，不是要设计掉的东西。 |

把这些全部减掉之后剩下的：一个**什么都留着**、**给一切定序**、**不命令任何人**的介质。

### 1.3 推导

这个设计不是一组选择。下面每一步都被上一步逼出来，而整个协议就是剩下的东西。

**1. 观察必须写在双方都看得见的地方。** 如果 A 直接告诉 B，那么还没出生的 C 就学不到
它。所以存在一个介质，而且它是被**写入**的，不是被**穿过**的。

**2. 这个介质承载事实，不承载命令。** 命令点名收件人，也就带来了投递语义：*收到了吗、
谁收到的、人不在了怎么办*。介质一旦欠了任何人一次投递，它就同时欠了活性、重试和身份，
它已经变成了一个挂着编排器的队列。而事实只点名世界。于是 `refs` 指向的是**一条事实，
或世界的一部分 —— 绝不是任何一方**，并且协议里没有任何字段可以写下收件人 —— 哪怕作者
存心想写（§5.4）。

*（这是结构性的论证。那个更弱的说法 —— 「寻址无法表达」 —— 是错的：作者可以往 payload
里塞任何东西。真正无法表达的是投递义务，因为协议里没有任何东西会代表谁去读一条事实。）*

**3. 光写下观察还不够，读者还得能算出「现在」。** 两个智能体都往 `deploy:prod` 写读数。
必须有东西决定哪个是当前的，而且它必须是双方各自计算的**规则**，不能是某人持有的**值**
—— 否则我们又回到了共享数据库（§1.2）。于是有了**折叠**：从日志到答案的纯函数。

**4. 折叠只有在输入有序时才是共享的。** 两个读者以不同顺序折叠同一*集合*的事实，会得到
不同答案。于是介质赋予**全序**，而这也是介质唯一决定的事。

**5. 这个序必须是被见证的，不是被自称的。** 如果作者自己给事实编号，它们会撞号、会撒谎、
会乱序。于是由单一组件赋予 `seq`，同一个组件盖上它见到每条事实的时刻（`recv`）并对这两
者签名。这个组件就是**总线**，而这几乎就是它做的全部。

**6. 任何东西都不得被改写。** 一个「过去会变」的日志上的折叠，不是任何东西的函数。于是
日志是**只追加**的、事实是**不可变**的；纠正是一条新事实去取代（§8.1），删除是一条新
事实去撤回（§10.1）。

**7. 身份必须来自内容。** 不可变的记录需要一个不会与所指之物脱钩的名字，而两个素未谋面
的智能体必须能就「我们手上是不是同一条事实」达成一致。于是
`id = sha256(canonical(content))` —— 内容地址（§5.9）。同样内容的重发就是同一条事实，
这顺带让追加天然幂等（§9.4）。

**8. 到这里，「谁负责」已经被解决了。** 问「谁在处理 F」，答案和别的折叠一样：F 上 seq
最小的存活领取。因为序是全的，每个读者不问任何人就算出同一个赢家。「一件活只做一次」是
**共享一个世界的推论**，不是这个设计的目的（§9.1）。

这就是全部协议：一条有序、只追加、由不可变的内容寻址事实组成的日志，一个定序并见证的
总线，以及承载全部含义的读者折叠。

**四个问题。** 一个隔离的智能体从共享世界里需要的一切，都是下面四问之一，而每一问都是
对同一条流的一次折叠。全文按一个智能体真正发问的顺序呈现它们：

| # | 问题 | 折叠 | § |
|---|---|---|---|
| 1 | **X 现在是什么** | subject 寄存器 | §8.1 |
| 2 | **它怎么来的 · 它引发了什么** | 因果踪迹 | §8.2 |
| 3 | **该不该信它** | 信任 | §8.3 |
| 4 | **谁对它负责** | 所有权 | §8.4 |

第 4 问是**推论，不是目的**。对一条事实负责，不过是世界状态的又一片；而因为世界是全序
的，这个答案恰好没有歧义 —— 于是白得了「一件活只做一次」（§9.1）。这个结果确实有用，
在本文里也是完全规范性的，但它是这条日志**最后**才为之存在的东西，不是最先。一条里面
一个 `_.claim` 都没有的流，照样是个完好的世界。

### 1.4 本规范的贡献

1. 一个作者域／总线域严格二分的**事实**（§4.2）—— 内容寻址、幂等、以及 `ts` 与 `recv`
   之间的信任不对称，全都由此**推导**出来，而不是被逐条规定。
2. **四条规范性折叠**（§8），覆盖一个隔离智能体真正会问的四个问题，定义在完整日志前缀
   之上，配有不变式与状态转移表。
3. 一个**授权模型**（§10.1），逐个 `refs` 键说明该链接是自断言的还是需要折叠门控的 ——
   因而也说明了一个作者能对别人的事实做什么、不能做什么。
4. 互斥性、确定性、单调性、幂等性的**证明**（§9），每条都暴露前提、标明边界。
5. 一套**一致性向量**（§A），钉住的是折叠而不只是哈希 —— 因为含义在折叠里。

### 1.5 非目标

写下这些不是谦虚；每一条都是这个设计**刻意没有买**的东西，而一个悄悄把它加回去的实现，
就不是这个协议了。

- **不是消息队列。** 没有东西被消费，没有东西被投递，没有事实是发给谁的。没有消费者组，
  也没有确认。
- **不是编排器或工作流引擎。** 日志里没有步骤、没有指派、没有调度器、没有计划。流水线
  是读者事后从踪迹里折出来的形状，绝不是谁持有的状态。
- **不是智能体框架。** 本协议对智能体如何决策、跑什么模型、怎么部署，一个字都不说。
- **不是共识协议。** 只有一条总线（§2.4）。本协议不在互不信任的副本之间复制一个决定，
  而 §8.3 的信任折叠明确**不是**共识 —— quorum 是读者的策略（§8.3）。
- **不是身份或认证系统。** `author` 是一个自断言的字符串（§10.1）。认证写入方是传输层
  的事。
- **不是权限系统。** 只要字段值域允许，任何写入方都可以写任何东西。§10.1 的门控约束的
  是一条事实**对读者意味着什么**，从不约束谁可以追加。
- **不是大对象存储。** `payload` 有上限（§B）；大内容存在别处，从 payload 里指过去。

---

## 2. 系统模型

本协议保证的一切，都是**相对于这个模型**才成立的。一个违反了这里某条假设的部署，不会
得到 §9 保证的降级版本 —— 它一条都得不到，而 §9 会说明是哪一条。

### 2.1 参与者

| 参与者 | 是什么 | 可以做什么 |
|---|---|---|
| **作者** | 任何执行追加的进程。由自断言的 `author` 字符串标识。 | 提交一条事实以供追加。仅此而已 —— 作者没有任何特权操作，协议也不区分任何作者。 |
| **总线** | 唯一的可信组件。 | 赋予 `seq`、盖 `recv`、签名、持久化、提供区间读取。§1.3 的四项职责，不多一件。 |
| **读者** | 任何读取并折叠的进程。 | 按游标取一段前缀并求值 §8。读者向总线要字节，从不要含义。 |

通常一个进程同时是这三者。这里把角色分开，是因为对每个角色的信任陈述不同，不是因为部署
形态不同。

### 2.2 信任边界

这条边界从每一条事实的中间穿过：

- **作者域字段不可信。** `type`、`author`、`ts`、`payload`、`refs`、`nonce` 就是写入方
  敲进去的东西。总线不判断它们的真伪，也 MUST NOT 因为一条事实*说错了*而拒绝它 —— 只
  因为它*格式不合法*才拒（§10.2）。读者 MUST NOT 把其中任何一个当作已被证实的。
- **总线域字段可信。** `seq`、`recv`、`sig` 由总线以自己的权威赋予，`sig` 就是证明。
  一切基于时间的折叠都以 `recv` 为准、绝不以 `ts` 为准，原因正在于此（§5.6/§5.7）。
- **`id` 的可信方式不一样。** 它既不是断言的也不是见证的，而是**可复算的**。任何持有
  该记录的人都能在不信任任何人的情况下验证它 —— 这就是为什么保护 `payload` 的是它，
  而不是 `sig`（§5.10）。

由此得出的推论只说一次，然后处处适用：**§8 中的每一条保证都相对于 `author` 的诚实而
言，唯有 §8.4 的定序结果例外 —— 那一条无论如何都成立**（§9.1）。

### 2.3 故障模型

**假设会发生，并且被处理：**

| 故障 | 协议如何挺过去 |
|---|---|
| 作者干到一半崩了 | 它的领取在 Δ 后失效、活可以重新分派，且不会撤销一个已完成的 resolve（§8.4、§9.3） |
| 作者追加到一半崩了 | 追加按 `id` 幂等，重试就是同一条事实（§9.4） |
| 网络丢包或重复请求 | 同上；重复是一次空操作，返回原来的回执 |
| 总线崩了 | 日志就是状态；恢复重放它，只重建纯投影（§11.1） |
| 总线写到一半崩了 | 撕裂的尾部被截断到最后一条能解析的记录（§11.1） |
| 读者任意慢或重启 | 折叠是前缀的函数，读者从游标续上并收敛（§9.2） |
| 时钟不同步或不准 | 没有任何折叠读 `ts`；`recv` 出自同一个时钟（§5.6/§5.7、§9.1） |
| 作者在 `payload` 里撒谎 | 不阻止。但会被暴露：§8.3 让别的作者去反驳它 |
| 作者伪造 `author` | 不阻止，且 §8.3 在这种情况下作废（§2.2、§12） |
| 磁盘上的 payload 损坏 | 恢复时复验 `id` 即可发现，并被报告（§11.1） |

**假设会发生，但不处理：**

- **拜占庭总线。** 总线被信任会诚实定序、不会伪造。本协议里没有任何东西能识别一个乱序、
  丢弃或凭空捏造事实的总线。客户端甚至无法验证 `sig`（§5.10）。
- **总线的网络分区。** 只有一条总线（§2.4）。分区时另一侧的写入方无法追加。本协议对这
  种情况不提供任何可用性方案。
- **作者之间的保密边界。** 日志的每个读者都读得到全部内容。
- **作者之间的时钟同步。** 不假设，也不需要 —— 这个设计的答案是折叠时用 `recv`，而不是
  去要求时钟同步。
- **读者资源耗尽。** §8.0 要求任何规范性结果都建立在完整前缀上，§11.2 又禁止压缩改变任何
  折叠的答案 —— 于是它只回收 payload，永远不回收骨架。两条合在一起给每个合规读者垫了一条
  地板：它必须保留有史以来每一条事实的骨架，而这条地板随日志的年龄单调抬升，本协议里没有
  任何东西为它设界。折叠同样是定义在整个前缀上的，所以一个读者对 N 条事实的日志回答 K 个
  问题就是 O(K·N) 的工作量 —— 除非它把折叠实现成增量的。这是 §2.2 那道切分的价钱：无状态
  的总线是靠让每个读者自己扛起整个世界来买到「一致」的。把它列进故障模型，是因为长寿命的
  部署会在生产里撞见它，而不是在这里读到它。

**缓解手段，且都不违反规范。** §8 规定的是答案不是算法（§8.0），所以只要返回值仍是 §8 说
的那个，读者 MAY 用任何方式算出来：

- **增量折叠。** §8 的每一条折叠都建立在读者可以随事实到达而在线维护的索引之上 —— 每个
  subject 的最高 seq 成员、每个目标的活跃领取、每个目标的票、每个 parent 的孩子 —— 而不必
  为每个问题重扫一遍前缀。单次提问的代价于是从 O(N) 降到答案本身的大小。
- **给派生状态打检查点。** 读者 MAY 把这份状态在 `seq` N 处落盘、从 N+1 续上。检查点是私有
  的派生数据，永远不是一条事实；且它只对算它时所用的参数有效：Δ 或它依赖的 quorum 一旦变了，
  读者 MUST 丢弃并重算（§B）。
- **共享一个更小的世界。** §2.4 允许为互相独立的 subject 空间开各自独立的日志。三条里只有
  这一条降的是地板而不是工作量 —— 分开之后，读者保留的是一个世界，而不是全部世界。

这三条都不允许的是：折一个被过滤、抽样或带缺口的窗口，然后把结果当作规范性答案端出去（§8.0）。

### 2.4 单总线假设及其边界

**一条日志，一个总线，一个全序。** §9 的每一条保证都压在这上面。它是整个设计的承重假设，
写在这里而不是隐含着，因为它划定了这个协议可以用在哪儿。

**它的代价：** 总线是单点故障，也是吞吐上限。需要多于一条总线的部署，拥有的是**多于一
个世界**，而本协议对如何调和它们只字不提。

**它买到的：** 没有共识轮次、没有向量时钟、没有冲突消解、没有主节点选举 —— 并且因为由
单一写入方赋予稠密全序，互斥变成了算术，而不是一把锁（§9.1）。

有两种扩展保住了这个假设，是被允许的：

- **只读副本。** 一个按 `seq` 顺序接收日志并提供读取的副本是可靠的：折叠是前缀的纯函数，
  一个落后 N 条的副本就是一个处在更早前缀上的读者，而 §9.2 已经覆盖了这种情况。副本
  MUST NOT 接受追加。
- **按世界分片。** 为互不相干的 subject 空间开独立日志是可以的，那就是几个独立的世界。
  一次折叠 MUST NOT 跨两条日志：`seq` 只在一条日志内部有意义，跨日志根本没有序可折。

任何让两个组件为同一条日志赋予 `seq` 的做法，都是另一个协议，本文不描述它。

---

## 3. 黑板与其上的单元

### 3.1 黑板

心智模型就是经典的**黑板架构**，减去它的控制组件。

黑板是一块共享的板面，各路专家把中间结果写上去；每个专家盯着板面，看到自己能处理的东西
就补一笔。没有人被点名。在 1970 年代的表述里，有一个*控制组件*决定下一个跑哪个专家 ——
而那个组件恰恰是跨组织时不存在的「更大的智能体」（§1.2）。这里改由**全序来仲裁**：经典
黑板需要一个调度器来防止两个专家干同一件活，而定序让这个答案变成每个专家可以各自算出来
的东西（§9.1）。

同样的形状在自然界叫**共识主动性（stigmergy）**：一只蚂蚁在地上留下信息素，其他每只蚂蚁
读地面。没有蚂蚁指挥另一只蚂蚁；介质承载状态，协调是「读同一块介质」的一个后果。这是本
协议的指导意象，而且是严格的 —— 任何会让一个参与者去指挥另一个的东西，在构造上就出局了。

### 3.2 事实

> **事实**是一条不可变、内容寻址的记录，记的是某个作者观察到或决定了什么，并由一个可信
> 总线放在单一全序中的唯一位置上。

四条性质，每条都在干活：

- **不可变** —— 于是折叠是日志的函数，而不是「什么时候跑」的函数。
- **内容寻址** —— 于是它的名字不会与内容脱钩，两个素未谋面的智能体能就「是不是同一条」
  达成一致。
- **位置唯一** —— 于是任意两条事实之间都有确定的先后，而这正是互斥可计算的原因。
- **是陈述，不是指令** —— 于是日志上没有任何东西是欠谁的。

事实不是消息、不是事件、不是任务、不是数据库的一行。它是「某件事被观察到或被决定了」的
陈述，而且即便后来被取代或被撤回，「它曾被陈述过」依然为真。

### 3.3 术语表

全文中这些词只在下述意义上使用。

| 术语 | 定义 |
|---|---|
| **事实（fact）** | §3.2。唯一的本原。 |
| **总线（bus）** | 唯一赋予全序的那个组件（§2.1）。 |
| **作者（author）** | 标识谁追加了一条事实的自断言字符串；也指那个进程本身。 |
| **读者（reader）** | 任何在求值折叠的进程。不是总线知道的角色。 |
| **日志（log）** | 全部事实的全序、只追加序列。 |
| **前缀（prefix）** | 所有 `1 ≤ seq ≤ N` 的事实。每次折叠的定义域（§8.0）。 |
| **完整前缀** | 没有缺口的前缀。折叠别的东西只能得到非规范性的近似。 |
| **折叠（fold）** | 从完整前缀（加参数）到答案的纯函数。全部含义所在。 |
| **寄存器（register）** | 共享同一个 `refs.subject` 的事实集合，以及从中折出的最新值（§8.1）。 |
| **subject** | **世界名** —— 一个不透明字符串，命名世界的一部分，由各方线下约定。不是事实 id。 |
| **踪迹（trail）** | `refs.parent` 的传递结构，向后走（`chain`）或向前走（`descendants`）（§8.2）。 |
| **缺口标记（gap marker）** | 一个显式占位，表示某个祖先不在本前缀里（§8.2）。 |
| **领取（claim）** | 一条带 `refs.claim_of` 的事实，断言对目标负责。 |
| **领取胜出者** | 目标上 seq 最小的存活领取的作者（§8.4）。 |
| **存活领取** | 尚未在 Δ 下过期、未被释放、且目标尚未被 resolve 或撤回的领取。 |
| **Δ（delta）** | 领取超时，单位秒。日志的属性，由总线发布（§B）。 |
| **被取代（superseded）** | 被一个后继替换。寄存器往前走；事实本身留在日志上（§8.1）。 |
| **被撤回（retracted）** | 被它**自己的作者**用 `_.tombstone` 收回。与「被取代」不同（§10.1）。 |
| **生命周期 ref** | `claim_of`、`resolves`、`release_of`、`tombstones` 之一。一条事实最多带一个（§5.4）。 |
| **作者域** | 作者写入的字段：进入内容哈希，不可信（§4.2）。 |
| **总线域** | 总线写入的字段：不进入哈希，可信（§4.2）。 |
| **内容地址** | `id` —— 对作者域求 `sha256(JCS(record))`（§5.9）。 |
| **终态** | 后续任何事实都无法改变的生命周期状态：`resolved`、`dead`（§8.4）。 |
| **合规读者** | 在完整前缀上、用总线发布的 Δ、精确实现 §8.1–§8.4 的读者（§A.1）。 |

---
## 4. 事实的结构

### 4.1 字段总览

```jsonc
{
  "seq":    1337,            // 总线：全序中的位置
  "recv":   1748300000.4,    // 总线：见证到达的时刻
  "id":     "b3f1…",         // sha256(JCS(作者域字段))
  "type":   "deploy.status", // 作者：点分类目
  "author": "ci@build-7",    // 作者：谁说的
  "ts":     1748300000,      // 作者：它说事情发生的时刻（仅供参考）
  "payload": { "v": 42 },    // 作者：它说了什么
  "refs":   { "subject": "deploy:prod" },  // 作者：这是关于什么的
  "nonce":  "k7x9",          // 作者：可选，用来换一个不同的 id
  "sig":    "hmac…"          // 总线：对头部的签名
}
```

| 字段 | 谁写 | 必需性 | 进哈希 | 可信度 |
|---|---|---|---|---|
| `type` | 作者 | REQUIRED | 是 | 不可信 |
| `author` | 作者 | REQUIRED | 是 | 不可信 |
| `ts` | 作者 | REQUIRED | 是 | 不可信 |
| `payload` | 作者 | REQUIRED | 是 | 不可信 |
| `refs` | 作者 | OPTIONAL | 非空时进 | 不可信 |
| `nonce` | 作者 | OPTIONAL | 存在时进 | 不可信 |
| `id` | 均可 | 落盘事实 REQUIRED | ——（它*就是*哈希） | **可复算** |
| `seq` | 总线 | 追加时赋予 | 否 | 可信 |
| `recv` | 总线 | 追加时赋予 | 否 | 可信 |
| `sig` | 总线 | 追加时赋予 | 否 | 可信 |

### 4.2 两个域

这九个字段不是一张平铺的列表，而是一个**二分**，并且这个二分是整个格式的组织原则：

> **黑板上的一行字，由两只手写成。**
> 作者写下内容；总线在旁边盖一枚章。

| | **作者域** | **总线域** |
|---|---|---|
| 字段 | `type` `author` `ts` `payload` `refs` `nonce` | `seq` `recv` `sig` |
| 进入内容哈希 | **是** | **否** |
| 可信度 | **不可信**（可伪造） | **可信**（总线签名） |
| 何时确定 | 追加之前 | 追加之时 |

这一条二分把格式里最难讲清的四件事一次讲通 —— 而它们不是四条规则，是同一条规则的四个
推论：

| 问题 | 二分直接给出的答案 |
|---|---|
| 为什么 `id` 只哈希一部分字段？ | 总线域必须在哈希之外。否则 `seq` 依赖 `id`、`id` 又依赖 `seq` —— 一个没有不动点的循环定义。 |
| 为什么重发会去重？ | 作者域相同 ⟹ `id` 相同 ⟹ 总线认出是同一条事实（§9.4）。 |
| `nonce` 为什么存在？ | 它是作者**主动改变作者域**、以在其余内容完全相同时换取一个新 `id` 的唯一手段。 |
| 为什么 `ts` 仅供参考而 `recv` 权威？ | 一个在作者域，一个在总线域。这不是「建议优先用 recv」，而是**结构上只有 recv 可信**（§5.7）。 |

`id` 有意地不属于任何一个域：它是作者域的一个*函数*，所以它既不是断言的也不是见证的，
而是**任何人都能复算的**。这就是为什么保护 `payload` 的是 `id` 而不是 `sig`（§5.10）。

---

## 5. 字段规范

每个字段都用同样的六项属性给出，随后是语义、示例、设计依据与约束。设计依据写成**反证**
而不是陈述 —— *删掉它会在哪一行崩掉* —— 因为这是唯一能同时告诉实现者「哪些可以妥协、
哪些一动就全盘失效」的形式。

### 5.1 `type`

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 | 上限 |
|---|---|---|---|---|---|
| 字符串 | 作者 | REQUIRED | 是 | 不可信 | ≤ 256 字节 UTF-8 |

**语义。** 一个点分类目标签，命名这条陈述的*种类*，让读者不解析 payload 就能廉价地过滤
一条流。

**示例。** `"deploy.status"`、`"build.failed"`、`"_.claim"`。

**设计依据。** 删掉 `type`，每个读者就必须解析每个 `payload` 才能判断自己关不关心 ——
§7.3 的读取过滤直接垮掉，一条本可以跳过的流变成一条必须反序列化的流。改成完全自由格式
而不加约束，glob 过滤就变得有歧义：当 `type` 里可以出现 `*` 或换行时，`deploy.*` 无法
表示任何确定的东西。v2.0 根本没有字符规则，于是一个数字型 `type` 让好几条折叠崩溃。

**保留命名空间。** 下列前缀由本协议保留。作者 MUST NOT 在其下造出这里未定义的类型，
读者也 MUST NOT 赋予它们应用含义：

| 前缀 | 归属 | 已定义成员 |
|---|---|---|
| `_.` | 本协议 | `_.claim`、`_.resolve`、`_.release`、`_.vote`、`_.tombstone` |
| `sys.` | 运维约定 | `sys.registry`（§8.5） |
| `context.` | 澄清约定 | `context.requested`、`context.provided`（§8.5） |

**约束。**

- 作者 MUST 提供 `type`。
- MUST 是非空字符串，由匹配 `[A-Za-z0-9_-]+(\.[A-Za-z0-9_-]+)*` 的点分段组成，最长
  256 字节 UTF-8。
- 总线 MUST 以 `400` 拒绝违例（仅超长时用 `413`）。
- 以保留类型为键的折叠 MUST 以 `type` 字段为键，而不能只看某个 `refs` 键在不在：
  `_.tombstone` 由 `type == "_.tombstone"` **且** 有 `refs.tombstones` 识别；`_.vote`
  由 `type == "_.vote"` **且** 有 `refs.vote` 识别。领取、解决、释放由各自的生命周期
  ref 识别，其 `type` SHOULD 是保留的那个，但 MUST NOT 被强制要求。
- 保留命名空间下的事实**不是** subject 寄存器的成员（§8.1）。

### 5.2 `author`

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 | 上限 |
|---|---|---|---|---|---|
| 字符串 | 作者 | REQUIRED | 是 | **不可信** | ≤ 256 字节 UTF-8 |

**语义。** 一个不透明字符串，命名是谁追加了这条事实。协议不施加任何结构，也不做任何认证
（§10.1、§12）。

**示例。** `"ci@build-7"`、`"sensor-3@node-a"`。

**设计依据。** 删掉 `author`，三条折叠会同时失去意义：§8.3 无法忽略自投票、也无法数清
不同的投票者；§8.4 无法判断谁的领取赢了、也无法判断一个 resolve 是不是来自胜出者；而
§10.1 的门控 —— 它们全都是「只有目标的作者可以……」的形式 —— 没有东西可比。反过来改成
由总线赋予而不是作者自断言，协议就凭空长出一个身份系统、一个凭据库和一套轮换方案，而这
些都不属于一个「全部职责就是定序」的组件。

**约束。**

- 作者 MUST 提供 `author`；MUST 非空且不超过 256 字节。
- 除值域检查外，总线 MUST NOT 解释、校验或改写它。部署 MAY 在前面放一个代理去盖章或
  校验它（§12）。
- 一个身份 SHOULD 对应一个进程。两个进程共用一个 `author` 并不被禁止 —— 总线看不出来
  —— 但它们会为自己的领取相互争抢，而折叠 §8.4 的读者会看到一个作者仿佛持有并释放了
  它从未做过的活。
- 读者 MUST NOT 把 `author` 当作已被证实的（§2.2）。

### 5.3 `payload`

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 | 上限 |
|---|---|---|---|---|---|
| JSON 对象 | 作者 | REQUIRED | 是 | 不可信 | 序列化后 ≤ 1 MiB |

**语义。** 这条陈述的内容。它的形状完全是应用自己的事；协议只读一个 payload 的一个字段
（`_.vote` 上的 `verdict`，§8.3）。

**示例。** `{ "v": 42, "region": "eu-west-1" }`。空对象 `{}` 合法且常见 —— 一条
`_.claim` 什么都不带。

**设计依据。** 给 `payload` 加 schema，协议就变成了一个数据模型，而不同厂商的智能体永远
不会就数据模型达成一致。允许非对象（裸字符串、数字、`null`），每个读者在碰它之前都得先
做类型检查，扩展没有地方安放，而 `{}` —— 压倒性最常见的情形 —— 不再是显而易见的空值。
v2.0 在这里允许任意 JSON，结果那些 `null` payload 弄坏了若干合理地假设这里是容器的折叠。

**约束。**

- MUST 是 JSON **对象**。非对象 MUST 以 `400` 拒绝。
- 缺省等价于 `{}`。
- 序列化后大小 MUST 不超过 §B 的上限；超出 MUST 以 `413` 拒绝。
- 扩展 MUST 放进 `payload`。总线 MUST 忽略且 MUST NOT 存储未知的**顶层**字段，作者也
  MUST NOT 指望它们能活下来。
- 读者可以推导出来的值 —— 生命周期状态、领取持有者、投票计数、取代状态 —— MUST NOT 被
  当作权威写在这里。读者 MUST 从 §8 计算这些，且 MUST NOT 信任任何声称给出其中之一的
  payload 字段。这是一条撰写规则，机器无法检查。

### 5.4 `refs` —— 唯一的关系机制

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 | 上限 |
|---|---|---|---|---|---|
| 字符串对象 | 作者 | OPTIONAL | 非空时进 | 不可信 | ≤ 64 个键 |

**语义。** 每个值命名的都是**一条事实，或世界的一部分。没有一个命名任何一方。** 这就是
「没有命令」的结构性原因（§1.3 第 2 步）。

**示例。**

```jsonc
"refs": { "subject": "deploy:prod", "parent": "b3f1…", "supersedes": "a0c2…" }
```

**已定义的键。**

| 键 | 值 | 含义 | 谁可以写 | 读者门控 |
|---|---|---|---|---|
| `parent` | 事实 id | 这条事实由那条引起；因果是 `parent` 的传递闭包（§8.2） | 任何人 | 无 —— 自断言 |
| `subject` | **世界名** | 命名这条事实所关于的那片世界；寄存器的分组键（§8.1） | 任何人 | 无 |
| `supersedes` | 事实 id | 这条事实**替换**目标 | 仅目标的作者 | §10.1 |
| `tombstones` | 事实 id | 在 `_.tombstone` 上：目标被**撤回** | 仅目标的作者 | §10.1 |
| `vote` | 事实 id | 配 `payload.verdict ∈ {corroborate, contradict}`（§8.3） | 目标作者之外的任何人 | §10.1 |
| `claim_of` | 事实 id | 作者断言对目标负责（§8.4） | 任何人 | 由序门控（§8.4） |
| `resolves` | 事实 id | 目标已处理；`payload` MAY 携带结果 | 仅当前领取胜出者 | §10.1 |
| `release_of` | 事实 id | 作者放弃自己此前的领取 | 仅持有领取的作者 | §10.1 |
| `about` | 事实 id | `context.requested`：那条被认为太单薄、无法据以行动的事实（§8.5） | 任何人 | 无 |
| `answers` | 事实 id | `context.provided`：它所回答的那个请求（§8.5） | 任何人 | 无 |

**`subject` 是世界名，不是事实 id。** 一个不透明的非空字符串，最长 256 字节，由线下就它
达成一致的写入方选定（`deploy:prod`、`schema:orders`、`belief:customer-42:churn-risk`）。
两个素未谋面的智能体靠约定一个名字来写同一片世界 —— 而这恰恰是它不能是内容地址的原因：
内容地址只有在内容存在之后才可知，而它们需要**事先**就约定好。

**生命周期 ref。** `claim_of`、`resolves`、`release_of`、`tombstones` 是生命周期 ref。
**一条事实 MUST NOT 携带多于一个**，总线 MUST 拒绝违例。

**设计依据。** 删掉 `refs`，就没有办法说一条事实是*关于*另一条的 —— 没有纠正、没有因果、
没有领取、没有投票 —— 日志变成一堆互不相干的陈述。让 `refs` 的值可以命名一个*智能体*，
「只有事实、没有命令」这条性质一行就死了：`{"to": "worker-3"}` 就是命令，投递那一整套
随之回流（§1.3）。允许一条事实带多个生命周期 ref，§8.4 的折叠顺序就有了歧义 —— 一条
既领取又解决同一目标的事实没有确定效果，两个读者可以正当地分岔。允许空值或 null，内容
地址就依赖于一条归一化规则：v2.0 在哈希时静默丢弃它们，而任何第二个实现都无从知道要照
着做。

**约束。**

- 若存在，MUST 是 JSON 对象；缺省等价于 `{}`。
- 每个值 MUST 是非空字符串。`null` 和 `""` MUST 被**拒绝**，而不是被丢弃。
- 最多 64 个键（§B）；`refs.subject` 最长 256 字节。
- 每条事实最多一个生命周期 ref；总线 MUST 以 `400` 拒绝违例。
- 总线 MUST 接受未知键（前向兼容）且 MUST NOT 解释它们。可信核心只读 `refs.parent`，
  而且只为执行 §10.2。
- 读者 MUST 忽略自己不理解的键。
- 作者 SHOULD 命名世界的一部分，SHOULD NOT 命名收件人。

### 5.5 `nonce`

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 | 上限 |
|---|---|---|---|---|---|
| 字符串 | 作者 | OPTIONAL | 存在时进 | 不可信 | ≤ 128 字节 |

**语义。** 一个唯一性令牌，它唯一的用途是：当其余内容完全相同时，改变作者域、从而改变
`id`。

**示例。** 早先的领取失效之后，对同一目标重新领取时用 `"k7x9"`。

**设计依据。** 删掉 `nonce`，幂等就变成一个陷阱。追加按 `id` 幂等（§9.4），这正是「网络
故障后重试是安全的」的来源 —— 但它同时意味着一次*正当的重复*会静默地塌缩进原来那条：
释放之后重新领取 F，或者在有了新证据之后再投一次同样的票，会产生逐字节相同的内容、因而
是同一条事实，于是那个新动作根本没有发生，而作者被告知成功了。`nonce` 是作者说出「这是
一次新动作，不是一次重试」的唯一手段。反过来改成由总线生成，性质就反了：每次重发都成为
一条新事实，§9.4 的重试安全性荡然无存。

**约束。**

- 若存在，MUST 是非空字符串，最长 128 字节。`""` MUST 被拒绝。
- 带生命周期 ref 或 `vote` 的事实 SHOULD 总是带 `nonce`。
- 意图跨重启保持幂等的事实 —— 一次注册、一次定时节拍 —— SHOULD 带一个由其场合推导出的
  **稳定** nonce，这样重启会复现同一个 `id` 并去重。

### 5.6 / 5.7 `ts` 与 `recv` —— 成对规范

| | `ts` | `recv` |
|---|---|---|
| 类型 | number（unix 秒，可含小数） | number（unix 秒，可含小数） |
| 谁写 | **作者** | **总线** |
| 必需性 | REQUIRED | 追加时赋予 |
| 进哈希 | **是** | **否** |
| 可信度 | **仅供参考** | **可信** |

**语义。** `ts` 是作者*声称*事件发生的时刻。`recv` 是总线*见证*该事实到达的时刻。

**示例。** 一台时钟慢了四小时的机器报告构建失败：

```jsonc
{
  "type": "build.failed",
  "ts":   1748285600,     // 作者说 14:13 —— 它的钟是错的
  "recv": 1748300000.4,   // 总线见证 18:13 —— 这个才是真的
  "payload": { "job": "nightly", "exit": 1 }
}
```

**设计依据。** 两个时间戳看着冗余。删掉任何一个，协议都会坏：

- **只留 `ts`。** 一切基于时间的判定就建立在一个可伪造的数字上。一个作者 —— 恶意的，或
  仅仅是钟在漂 —— 把 `ts` 设成很久以前，自己的领取就立刻过期，从而绕开互斥；设成未来
  则能永久霸占一件活。**§9.1 失效**，而 §9.1 是本协议的核心结果。
- **只留 `recv`。** `recv` 不能进内容哈希（那会是循环的，§4.2），于是同样内容重发永远
  得到同一个 `id`，作者**无法表达「这是一次新动作」** —— §9.4 的逃生口没了。更糟的是，
  作者真正知道的时刻（一行日志的时间戳、一个文件的 mtime）无处安放，于是溯源失去了它在
  作者一侧唯一的时钟。

分工是刚性的：**`ts` 属于内容，`recv` 属于见证。**

**约束。**

- 作者 MUST 提供 `ts`。MUST 是有限的 IEEE-754 双精度；`NaN` 与 ±`Infinity` MUST 以
  `400` 拒绝 —— 因为非有限数在规范形式里不可表示（§5.9），这样一条事实的 `id` 将永远
  无法被复算出来。
- 总线 MUST NOT 因为 `ts` 不合理而拒绝追加。总线不判断内容（§2.2）。
- 总线 MUST 赋予 `recv`，并 MUST 将其纳入 `sig`（§5.10）。
- `recv` MUST 随 `seq` 单调不减。
- **一切以时间为条件的折叠 —— 领取过期（§8.4）与任何 TTL —— MUST 用 `recv`，MUST NOT
  用 `ts`。** 违反此条会同时破坏 §9.1 与 §9.2。
- 读者 MAY 向人展示 `ts`，但 SHOULD 标注其为作者自述。

### 5.8 `seq`

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 |
|---|---|---|---|---|
| 整数 ≥ 1 | 总线 | 追加时赋予 | 否 | 可信 |

**语义。** 这条事实在单一全序中的位置。总线唯一决定的东西。

**示例。** `1337`。日志中的第一条事实 `seq` 为 1，因此从 `since = 0` 起步的读者能看到
全部内容。

**设计依据。** 删掉 `seq` 就没有全序；折叠退化成*集合*的函数，两个读者可以正当地各执一
词，§9.1 与 §9.2 同时失效 —— 这个字段正是整个设计存在的理由。让作者自己赋值，它既不唯一
也不单调，无法据以判定先后。从时间戳推导，就可能撞值，于是互斥需要一条不再是算术的
tie-break 规则。

**约束。**

- 总线 MUST 从 1 开始，赋予稠密、严格递增的整数 `seq`。
- 总线 MUST NOT 复用任何 `seq`，**永远不**，包括在一次截断或修复之后（§11.1）。
- 恢复时，计数器 MUST 恢复为日志中出现过的最大 `seq`。

### 5.9 `id` —— 内容地址

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 |
|---|---|---|---|---|
| 小写十六进制 sha256 | 均可 | 落盘事实 REQUIRED | ——（它就是哈希） | **可复算** |

**语义。** 这条事实的名字，由它的内容算出，于是名字不会与所指之物脱钩，任何两方也能就
「我们手上是不是同一条事实」达成一致。

**计算方式。**

```
id = sha256( JCS( record ) )                    输出小写十六进制

record = { "type": …, "author": …, "ts": …, "payload": … }
       + "refs"： 仅当 refs 非空时出现
       + "nonce"：仅当 nonce 存在时出现
```

其中 `JCS` 是 **[RFC 8785，JSON 规范化方案](https://www.rfc-editor.org/rfc/rfc8785)**。
`seq`、`recv`、`sig` 以及 `id` 自身被排除在外 —— 它们是总线域（§4.2）。这里**没有归一化
步骤**：§5.1–§5.5 已经拒绝了一切需要归一化的东西，所以 record 就是提交上来的那些字段。

**示例。** 记录 `{"author":"u","payload":{},"ts":100,"type":"t"}` —— 注意键按 JCS 排序
且无空白 —— 哈希得到 §A 中 `whole-number-ts` 向量的 `id`。

**设计依据。** 改用总线赋予的名字，两个智能体就无法比较各自单独取得的事实，去重需要一次
往返，而 `parent` 链接指向的是一个位置而不是一份内容 —— 于是祖先可以被掉包而任何链接都
不变，§8.2 的防篡改性随之消失。

用散文描述规范化而不采用 JCS，你会得到 v2.0：它描述了一条 Python 兼容的浮点规则，而参考
实现只把它应用到了恰好一个键上，其余每个数字都听凭宿主语言的格式化器，键序与字符串转义
则根本没有规定。独立实现在小指数、大指数和非 BMP 键名上都分道扬镳。而 RFC 8785 已经把这
些全规定好了 —— 数字序列化（ECMAScript `Number::toString`，最短往返）、属性排序（按
**UTF-16 码元**）、字符串转义、空白（没有）—— 并且在任何一个会被拿来写第二实现的语言里
都有经过测试的库。采用它是把问题删掉，而不是重述一遍。

实现者必须注意的三个后果：

- **所有 v2.0 的 id 都变了。** JCS 不输出空白，也不把 `ts: 100` 渲染成 `100.0`。
- **键序是 UTF-16 码元序。** Python 实现 MUST NOT 用裸 `sorted()`：它按码点排序，在非
  BMP 键上给出不同答案 —— U+1F600 的码点比 U+FF3A 大，但它的 UTF-16 高位代理比后者小。
  §A 钉住了这个用例。
- **非有限数不可表示**；§5.6 已经拒绝了它们。

**约束。**

- 作者 MAY 提供 `id`。若提供，总线 MUST 复算并 MUST 以 `400` 拒绝不匹配。
- 落盘事实 MUST 携带 `id`。
- 总线 MUST 在恢复时复验 `id == sha256(JCS(record))`，并 MUST 报告失败计数（§7.5、
  §11.1）。
- 被压缩丢弃了 `payload` 的事实不再哈希到它自己的 `id`；它 MUST 被标记，读者 MUST NOT
  把它报为完整性失败（§11.2）。

### 5.10 `sig`

| 类型 | 谁写 | 必需性 | 进哈希 | 可信度 |
|---|---|---|---|---|
| 十六进制 hmac-sha256 | 总线 | 追加时赋予 | 否 | 可信 |

**语义。** 总线的证词：这条事实的位置和到达时刻是**它**赋予的，而不是某个编辑日志文件的
人写上去的。

```
sig = hmac_sha256( secret, "id|author|type|ts|recv|seq" )
```

其中 `ts` 与 `recv` 按 JCS 数字格式渲染，`seq` 按十进制整数渲染。

**设计依据。** 删掉 `sig`，只要有人能写到日志文件，总线域就和作者域一样可伪造：而 `seq`
与 `recv` 恰恰是 §9.1 和 §9.2 所依赖的那两个字段，所以一份可编辑的日志意味着一个可编辑
的赢家。反过来把它扩展到覆盖 `payload`，那会与 `id` 已经做的事重复 —— 同时制造一种幻觉，
让人以为 HTTP 客户端可以据此验证内容，而它做不到，因为密钥是对称的。

**运维必须理解的两个界限。**

- **`sig` 覆盖头部，不覆盖内容。** `payload` 与 `refs` 不在被签名的消息里；覆盖它们的是
  `id`，而恢复会复验 `id`（§11.1）。`sig` 验不过意味着头部被改动过，或者日志是在另一个
  密钥下写的。它**不**意味着 payload 完好。
- **分隔符是有歧义的。** `author: "a|b", type: "c"` 与 `author: "a", type: "b|c"` 产生
  相同的签名输入。由于该 HMAC 只在总线内部使用，这不构成认证绕过，但实现 SHOULD 对字段
  做长度前缀；未来版本会要求这样做。

**约束。**

- 总线 MUST 赋予 `sig`，并 MUST 在密钥稳定时于恢复期验证它，通过 §7.5 报告失败。
- 验证方 MUST 以常数时间比较。
- 密钥是对称的，因此**只有持有密钥的一方能验证**：总线本身，或共享密钥的副本。未认证的
  读者无法验证 `sig`；对它而言，`id` 是完整性检查，而 `seq`/`recv` 靠信任总线来信任。
- 运维 MUST 配置一个稳定的密钥。启动时自行生成临时密钥的总线 MUST 报告这一点（§7.5），
  因为重启之前写下的每一个签名都变得无法验证。

---
## 6. 完整实例：一件被争抢的活

这是一个完整回合，从头到尾，带真实的内容地址。下面每一个 `id` 都可复现：对作者域字段求
JCS 形式再哈希（§5.9），你会得到同一个字符串。下面每一个折叠结果也可复现，仅凭这条流。

场景：CI 报告构建失败。两个智能体都想接。一个赢了，干完活，报告结果。没有任何人被通知。

### 6.1 这条流

| # | seq | recv | 作者 | 事实 | `id` |
|---|---|---|---|---|---|
| 1 | 41 | 1748300000.4 | `ci-bot` | `build.failed` · payload `{job, exit}` | `7f2a743a3f35…` |
| 2 | 42 | 1748300012.1 | `agent-a` | `_.claim` · `claim_of: 7f2a…` · nonce `a1` | `4d5da059c140…` |
| 3 | 43 | 1748300012.9 | `agent-b` | `_.claim` · `claim_of: 7f2a…` · nonce `b1` | `c605e94b9a55…` |
| — | — | — | `agent-b` | *读回并折叠；发现自己输了* | *不产生事实* |
| — | — | — | `agent-a` | *逐字节重发 #42* | *被去重* |
| 4 | 44 | 1748300210.0 | `agent-a` | `_.claim` · `claim_of: 7f2a…` · nonce `a2` | `919131dd57b3…` |
| 5 | 45 | 1748300455.7 | `agent-a` | `fix.done` · `parent: 7f2a…` · `resolves: 7f2a…` · nonce `a3` | `a0266a551b88…` |

完整地址：

```
41  build.failed  7f2a743a3f3598755651b4c01d6d1fb2b3be5d09b0036545e87d6cfd2b17c45d
42  _.claim       4d5da059c140d54293a5952ffe9654cc03f5e4cc8103d2a2f4334ce06e47fdf9
43  _.claim       c605e94b9a550b05d83105bd4281183869ad501e2a21583cd801b8e5da47e1fd
44  _.claim       919131dd57b3df60aece064d814ae717fa61bc2d69e6143a5be767044463ef57
45  fix.done      a0266a551b884a3a8542e28b94d717bc61b05bdb4130ea06e718579aa2b92aad
```

事实 #41 被哈希时的样子 —— JCS 键序、无空白，`recv`/`seq`/`sig`/`id` 都不在，因为它们
是总线域（§4.2）：

```
{"author":"ci-bot","payload":{"exit":1,"job":"nightly"},"ts":1748300000,"type":"build.failed"}
```

### 6.2 每一步演示了什么

**1 · `ci-bot` 追加 `build.failed`（seq 41）。** 作者写了 `type`、`author`、`ts`、
`payload`；总线补上 `seq: 41`、`recv: 1748300000.4`、`sig`。`id` 只是前一组的函数，
所以它在追加之前就能算出来、在追加之后可以验证（§4.2、§5.9）。

注意这里**没有**什么：没有受理人，没有队列，没有「需要修」。它是一句关于世界的陈述。

**2 · `agent-a` 领取它（seq 42）。** 意图的表达方式和其他一切的表达方式一样 —— 追加一条
事实。领取不是一次加锁请求，总线也不裁决它；总线给它一个位置并存下来，和对待任何别的事实
完全一样。

**3 · `agent-b` 也领取它（seq 43）。** 没有报错，没有拒绝，没有串行化。两条领取都存在。
**42 < 43**，而这已经是全部答案了。

**4 · `agent-b` 读回并折叠。** 它在自己持有的前缀上算 `ownership(#41)`，得到
`claimed(agent-a)`。没有人告诉它。它没有收到拒绝消息，没有去轮询一个锁服务，也不需要
信任 `agent-a`：它从同一批有序字节里推出了 `agent-a` 推出的同一个答案（§9.1）。它转去
干别的活。

**5 · `agent-a` 逐字节重发 #42** —— 比如说，一次 socket 错误之后的重试。作者域相同 ⟹
`id` 相同 ⟹ 总线返回既有的 `{seq: 42, recv, sig}`，带 `deduped: true` 与 HTTP `200`。
**不存在第二条领取**，因此折叠的结论没有任何变化。这就是客户端可以在网络故障后直接重试
而无需思考的原因（§9.4）。

**6 · `agent-a` 用新的 nonce 续期（seq 44）。** 活干得比 Δ 长。`agent-a` 用
`nonce: "a2"` 追加同样的领取，这改变了作者域、因而改变了 `id`，所以这**是**一条新事实。
seq 42 那条会在 `recv + Δ` 失效；到那时，同一个作者的这一条就是 seq 最小的存活领取。
**所有权得以延续，没有锁，没有租约续期端点，也没有任何协议扩展** —— 重叠本身就是全部
机制。

**7 · `agent-a` 报告（seq 45）。** 一条事实同时带 `parent: #41` —— 这项工作由那次失败
引起（§8.2）—— 和 `resolves: #41` —— 那件事现在处理完了（§8.4）。它恰好带一个生命周期
ref，符合 §5.4；`parent` 不是生命周期 ref。

这个 resolve 之所以被采纳，是因为在折叠到那一点时 `agent-a` 是当前的领取胜出者。如果是
`agent-b` 追加它，或者根本没人领过 #41，折叠会忽略它（§8.4）。

**8 · 任何读者，在任何更晚的时刻，折出同样的结论。**

```
seq 42 之后  lifecycle(#41)  →  claimed(agent-a)
seq 43 之后  lifecycle(#41)  →  claimed(agent-a)      # b 的领取在日志上，且输了
seq 45 之后  lifecycle(#41)  →  resolved(agent-a)     # 终态
一天以后     lifecycle(#41)  →  resolved(agent-a)     # 仍是终态
chain(#45)                   →  7f2a743a3f35 → a0266a551b88
```

### 6.3 失败分支

现在假设 `agent-a` 在 seq 44 之后立刻挂了，从未报告。

它在 seq 44 的领取 `recv = 1748300210.0`。Δ = 600 时，它会在某条更晚事实的 `recv` 越过
`1748300810.0` 时失效。仍在盯着的 `agent-b` 在 `recv = 1748300811.0` 追加了一条新领取：

```
lifecycle(#41)  →  claimed(agent-b)
```

没有任何东西检测到那次崩溃。没有主管重新分派这件活。过期是两个总线盖章数字之间的算术，
所以每个读者 —— 包括 `agent-b` 自己 —— 都在同一点算出同一次交接（§9.1）。

而让这件事从「方便」变成「安全」的那条性质是：如果 `agent-a` 其实先**完成**了，
`agent-b` 那条同样迟到的领取什么也改不了。

```
… seq 45 由 agent-a 解决，随后 agent-b 在 recv + Δ + 1 处迟到领取
lifecycle(#41)  →  resolved(agent-a)
```

`resolved` 是终态，且在那条迟到领取被折叠之前就已经到达，所以崩溃恢复的重新分派永远无法
撤销一次真实的完成（§9.3）。

---

## 7. 操作

### 7.1 操作目录

只有两个操作。本节其余内容要么是第二个操作之上的便利，要么是协议之外的端点。

| 操作 | 方向 | 效果 | 幂等 |
|---|---|---|---|
| **append** | 作者 → 总线 | 赋予 `seq`/`recv`/`sig`，持久化，返回回执 | 是，按 `id`（§9.4） |
| **read** | 读者 ← 总线 | 从游标返回有序流的一个窗口 | 是（纯） |

刻意没有 `claim`、`resolve`、`vote` 或 `delete` 操作。它们每一个都是追加一条带相应 `refs`
键的事实，而其含义是一次折叠（§8）。一个提供 `claim` 端点的总线，已经把含义搬进了可信
核心，它实现的不是这个协议。

### 7.2 append 时序

```
POST /facts
{ "type": …, "author": …, "ts": …, "payload": …, "refs": …, "nonce": …, "id"?: … }
```

1. **按 §5.1–§5.6 校验作者域。** 失败时总线返回 `400`（§B 上限则 `413`），且 MUST NOT
   存储任何东西。
2. **计算 `id`** = `sha256(JCS(record))`（§5.9）。如果客户端提供了 `id` 且不一致，返回
   `400`。
3. **查去重索引。** 若 `id` 已存在，返回 `200` 与既有的 `{seq, recv, id, sig}` 及
   `deduped: true`。不写第二份，不消耗 `seq`。
4. **检查因果深度**（§10.2）。超限返回 `400`。
5. **赋值** `seq`（下一个整数）与 `recv`（此刻，且不早于上一条事实的 `recv`），并计算
   `sig`。
6. **按持久化策略落盘**（§11.1），然后更新内存投影。
7. **返回 `201`**，携带 `{seq, recv, id, sig, deduped: false}`。

第 5–6 步相对于其他追加 MUST 是原子的：一个 `seq` 永远只发给一份内容（§5.8）。

**总线不做什么。** 它不检查 `refs` 的目标是否存在 —— `parent` 或 `claim_of` 可以命名一条
还没到的事实，这正是客户端可以从叶子开始追加一条链的原因（§10.2）。它不求值任何折叠，
因此它永远不会因为一条领取来晚了、一个 resolve 来自错误的作者、或一次取代未经授权而拒绝
它们。那些是读者的事，而一个替它们做判断的总线就是在持有状态（§2.1）。

**状态码。**

| 码 | 含义 |
|---|---|
| `201` | 已追加 |
| `200` | 已存在；内容相同，返回既有回执 |
| `400` | 格式不合法：§5 值域违例、`id` 不匹配、多于一个生命周期 ref、或因果深度超限 |
| `413` | 超出 §B 上限 |
| `429` | 准入限流拒绝（若该部署启用，§10.3） |

### 7.3 read 时序

```
GET /facts?since=<seq>&limit=<n>&type=<glob>&author=<s>&refs.<key>=<id>
```

返回 `seq > since` 的事实，按 `seq` 升序，最多 `limit` 条。响应携带 `X-Max-Seq`，即窗口
中最大的 `seq`，读者把它存为下一次的 `since`。读者循环直到某次响应短于 `limit`。

所有过滤器都是**便利，不是语义**。被过滤的窗口不是完整前缀，所以在它上面的折叠不是规范性
的（§8.0）。总线 MAY 忽略每一个过滤器、返回未过滤的窗口；合规读者仍须能工作。过滤器的存在
是为了省字节，而需要规范性答案的读者必须持有前缀。

**`type` 的 glob 方言。** `*` 匹配任意长度的字符串，`?` 匹配恰好一个字符。此外没有任何
特殊字符。模式针对整个 `type` 匹配。总线 MUST 限制模式长度（§B），并 MUST 以 `400` 拒绝
过长的模式：这个模式是攻击者提供的。

**游标便利。**

| 端点 | 返回 |
|---|---|
| `GET /facts/head` | `{ head_seq }` —— 让读者从「只看新的」起步 |
| `GET /facts/:id` | 按内容地址取一条事实，或 `404` |

### 7.4 一次领活的完整交互时序

这是实现者必须做对的完整往返，也是「读回并折叠」唯一不可省略的地方。

```
智能体                                  总线
  │                                      │
  │  1. GET /facts?since=<游标>          │   追到一个完整前缀
  │ ───────────────────────────────────► │
  │ ◄─────────────────────────────────── │   facts、X-Max-Seq
  │                                      │
  │  2. 折叠：F 是 open 吗？              │   ownership(F) == open（§8.4）
  │     （本地算；不问总线）               │
  │                                      │
  │  3. POST /facts  _.claim             │   refs.claim_of = F，新 nonce
  │ ───────────────────────────────────► │
  │ ◄─────────────────────────────────── │   201 { seq: N }   ← 这不是授予
  │                                      │
  │  4. GET /facts?since=<游标>          │   MUST 重读到 N 之后
  │ ───────────────────────────────────► │
  │ ◄─────────────────────────────────── │
  │                                      │
  │  5. 折叠：claimWinner(F) == 我吗？    │   这才是决定
  │                                      │
  │     是 → 干活                         │
  │     否 → 转身走人；你输了，而且不会有   │
  │          任何消息来告诉你              │
  │                                      │
  │  6. POST /facts  fix.done            │   refs.resolves = F（可带 parent）
  │ ───────────────────────────────────► │
  │ ◄─────────────────────────────────── │   201
```

这段时序编码了四条规则：

1. **领取拿到 `201` 不是授予。** 它只表示「你的事实在日志的 seq N 上」。你是否赢了由第 5
   步决定，且只由它决定。把追加回执当作获取的实现有竞态。
2. **第 4 步必须重读到自己那条领取之后。** 在你的请求飞行途中，竞争者的领取可能被赋予了
   比你更小的 `seq`。
3. **第 5 步折叠的是每一条引用 F 的事实** —— `claim_of`、`release_of`、`resolves` 以及
   任何指向 F 的 `_.tombstone` —— 而不只是那些领取。像 `?refs.claim_of=<id>` 这样的单键
   过滤器**不充分**，总线也 SHOULD NOT 被描述成「让这件事变便宜了」：那个窗口藏起了释放、
   解决与撤回，于是它在两个方向上都出错 —— 会报出一个领取已被释放的胜出者，也会把已经被
   解决或已死的活报成可领。这个过滤器适合用来*找到* F 上的领取；判定胜出者是折叠的事，
   在完整前缀上。
4. **长时间持有者靠续期而不是靠释放**（§6.2 第 6 步）：每约 Δ/3 用新 nonce 重新领取一次。
   绝不要「先释放再重领」，那会打开一个这件活真的处于 `open` 的窗口。

### 7.5 协议之外的端点

总线 MAY 暴露运维端点。它们不是 wire 契约的一部分，合规读者 MUST NOT 依赖它们。

| 端点 | 用途 |
|---|---|
| `GET /info` | 生效配置与健康状况：协议版本、`head_seq`、fsync 策略、密钥是否稳定、`sig` 与 `id` 的失败计数，以及 **Δ** |
| `GET /health` | 存活性 |
| `POST /admin/rewrite` | 触发压缩（§11.2） |

一条硬规则：**运维端点 MUST NOT 改变任何 §8 折叠的结果。** Δ 在这里发布，是因为 §8.4
要求每个读者使用日志的值而不是自己的值（§8.4、§B）；那是发布，不是改动。

---

## 8. 折叠语义（规范性）

读者按 `seq` 顺序重放事实，把它们折成自己需要的任何投影。**一致性活在这里**：总线是无
状态的，所以一个把 §7 实现得完美、却把本章折叠得不一样的实现，不具备任何有用意义上的互
操作性。折叠方式相同的两个读者永远一致，因为它们消费的是同一条全序、不可变的流 —— 而这
就是全部要点。

### 8.0 折叠的定义域

> 本章每一个折叠都是日志的一个**完整前缀**的函数：所有 `1 ≤ seq ≤ N` 的事实。

读者 MUST 持有完整前缀才能声称一个规范性结果。在被过滤、被采样或含缺口的窗口上折叠是允许
的，但得到的是**非规范性的近似**，而实现 SHOULD NOT 把这样的结果呈现为本章定义的折叠。这条
要求让读者付出的代价、以及一个部署可以拿它怎么办，见 §2.3。

处在不同 N 上的两个读者当然可以不同 —— 一个比另一个看到了更多世界。那不是分歧，那是延迟。
分歧指的是处在**同一个** N 上的两个读者给出不同答案，而本章的存在就是为了让那不可能发生
（§9.2）。

**终止性。** 沿 `refs` 链接行走的折叠 MUST 在任何输入上终止，包括一个行为良好的总线绝不会
产生的流。实现 MUST 用访问集合或显式深度上限来限界每一次行走。不要依赖 §10.2 关于「环无法
构造」的论证：它对经由合规总线追加的事实成立，而折叠也会跑在导出的、复制的和手工修复过的
日志上。

§1.3 的四个问题，按一个隔离智能体真正需要的顺序。

### 8.1 X 现在是什么 —— subject 寄存器

一个 `refs.subject` 命名世界的一部分。每一条带该 subject 的事实都是关于它的一句陈述；它们
合起来就是这个 subject 的**寄存器**。一个不断变化的值就是这样在隔离的智能体之间被共享的，
而不需要任何人持有它：没有人存「当前值」，每个读者自己折出来。

```
retracted(x) = ∃ t ∈ prefix : t.type == "_.tombstone"
                              且 t.refs.tombstones == x.id
                              且 t.author == x.author              # §10.1

history(S) = [ f ∈ prefix : f.refs.subject == S
                            且 f.type 不在保留命名空间中（§5.1） ]，
             按 seq 升序

current(S):
  若 history(S) 为空                  → null
  h ← history(S) 中 seq 最大的事实
  返回 retracted(h) ? null : h                                    # 一无所知

supersededBy(F):                        # 那条**直接**替换了 F 的事实
  若 F ∉ prefix 或 retracted(F)       → null
  E ← [ x ∈ prefix : x.refs.supersedes == F.id                    # 显式后继，
                     且 x.author == F.author                      #   且已授权（§10.1）
                     且 not retracted(x) ]
  G ← [ x ∈ history(F.refs.subject) : x.seq > F.seq               # 寄存器里的下一条
                     且 not retracted(x) ]
  C ← E ∪ G
  返回 C 为空 ? null : C 中 seq **最小**的那条

isSuperseded(F) = supersededBy(F) != null
```

**不变式。** 对任意完整前缀 P 与 subject S：

| # | 不变式 |
|---|---|
| I1 | `current(S) ∈ history(S) ∪ {null}` —— 寄存器的值永远是寄存器的成员。 |
| I2 | `history(S)` 按 `seq` 严格递增，且只会追加增长；更长的前缀绝不移除成员。 |
| I3 | `supersededBy(F) = G` ⟹ `G.seq > F.seq`。取代永远只指向前方。 |
| I4 | `retracted(F)` ⟹ `¬isSuperseded(F)`。「被撤回」与「被取代」互斥。 |
| I5 | `current(S) ≠ null` ⟹ 在 P 内 `¬retracted(current(S))` 且 `¬isSuperseded(current(S))`。 |
| I6 | 对 `history(S)` 中 `F.seq < F′.seq` 且都未被撤回的 F、F′：`isSuperseded(F)`。每个非头部的存活成员都被下一条取代。 |

**六条规则使它在 v2.0 不确定的地方变得确定。**

1. **`current(S)` 只在 `history(S)` 上取值。** 一条想成为 S 当前值的事实 MUST 携带
   `refs.subject: S`。在 v2.0 里，显式后继可以不带 subject 就成为 `current(S)`，于是
   `current(S) ∉ history(S)` 是可达的，I1 失效。要说 X 现在是什么，就得*关于 X* 去说。
2. **`supersededBy` 返回直接后继** —— seq 最小的候选，不是最新的那条。「什么替换了 F」
   是下一句陈述；最新那句陈述是 `current(S)`。反复跟随 `supersededBy` 是在寄存器里一步
   一步向前走。
3. **平局由 `seq` 打破**，而 `seq` 是全序，所以从来不存在选择。若存在两条已授权的后继，
   seq 较小的那条是后继，另一条只是一条同样声称要替换 F 的普通事实。关心这种分叉的读者
   可以自己枚举 `E`。
4. **只有作者能取代自己的事实**（§10.1）。替换说的是*「我先前那句话过时了」*；第三方观察
   到陈旧，是去反驳（§8.3）或者往寄存器里写，而不是有权把别人的陈述退役。这对寄存器毫无
   代价 —— 那里的推进靠分组顺序，不靠显式 `supersedes`。

   **这道门控到底值多少，说准确。** 它保护的是**不在**任何寄存器里的事实。对这样的事实，
   `supersedes` 是通往 `superseded` 的唯一路径，因此给它加门控，就挡住了「任何作者一次
   追加就让另一个作者的信任状态噤声」——§8.3 里 `superseded` 压过每一张票。

   它**保护不了**带 `subject` 的事实，也不可能保护。寄存器是一片任何人都可以写的共享世界
   状态，正是这一点让它成其为「共享」（§5.4）；组内的下一个成员按分组顺序取代前一个，这条
   路径上**没有任何作者门控**。所以陌生人照样能推动寄存器头部，被顶下去的那条事实照样折叠
   为 `superseded` —— 走的正是本条规则上一句刚刚认可为「正当替代方案」的那条路。

   对读者而言值得挑明的是后果：**在寄存器内部，`superseded` 是关于寄存器的陈述，不是关于
   那条事实的陈述。** 它只意味着*后来有人又说了关于 X 的话*，而任何写入方都能造成这件事。
   要判断一条事实的内容可不可信，读者 MUST 去读 §8.3 的投票状态，MUST NOT 把 `superseded`
   当作「内容是错的」的证据。
5. **被撤回的后继什么也取代不了。** 如果那条替换了 F 的事实自己后来被撤回，
   `supersededBy(F)` 退回到下一个候选，然后退到 `null`。否则撤回一个糟糕的替换，会让原
   事实永远停在 `superseded` —— 而当前位置上什么都没有。
6. **保留命名空间的事实不是寄存器成员。** 给 `_.tombstone` 打上 `refs.subject` 是一个很
   自然的错误，而没有这条规则，那次撤回本身会成为 `current(S)`，并同时取代它所撤回的那条
   事实 —— 同时违反 I1 和 I4。撤回只通过 `refs.tombstones` 发生。

**撤回不是回滚。** 被撤回的寄存器头部折叠为 `null` —— *当前一无所知* —— 而不是折回上一个
值。把更旧的陈述复活，等于断言一件当下没有任何作者在断言的事。对非头部成员的撤回不改变
`current(S)`；它只把该成员标记为已撤回，供 §8.3 与 §11.2 使用。

**「最新者胜」只是读者的一种策略，不是唯一策略。** 一个累积多源观测的读者读 `history(S)`
而不去折叠它。这是寄存器被支持且被预期的用法；§11.2 通过禁止压缩销毁非头部 payload 来
保护它。

### 8.2 它怎么来的、又引发了什么 —— 踪迹

```
chain(F)       = 从 F 沿 refs.parent 传递地走到根，按 根→F 返回
descendants(F) = 每一条其 parent 链能到达 F 的事实，传递闭包，按 seq 排序，不含 F
depth(F)       = |chain(F)|            # 没有 parent 的事实深度为 1
```

两者都是同一前缀上的纯折叠，所以另一个节点上的读者会重建出同样的踪迹。对于不在前缀里的
`F`，`chain(F)` 为空，而 `descendants(F)` 返回那些把 `F` 写作 parent 的事实 —— 一条未见
事实**之下**的踪迹仍然可知，**之上**的不可知。实现 MUST NOT 把那条缺席的 `F` 报为根。

**缺口是显式的。** `refs.parent` MAY 命名一条不在前缀里的事实 —— 因为它还没到、被过滤掉
了，或者从来不存在。当一次行走触到这样的引用时，实现 MUST 把它作为一个显式的**缺口标记**
呈现出来、携带那个未解析的 id，且 MUST NOT 像到达了根一样静默停止。一条看起来完整、实际
被截断的链，比没有链更糟：它把「我看不到起源」变成了「这就是起源」。

**不变式。**

| # | 不变式 |
|---|---|
| I7 | `chain(F)` 有限且不含重复 `id` —— 由 §8.0 的访问集合保证，不是由总线保证。 |
| I8 | 当两者在 P 中都完全可解析时，`G ∈ descendants(F)` ⟺ `F ∈ chain(G)`。 |
| I9 | 两者对前缀单调：更长的前缀可以增加成员、可以把缺口标记换成事实，但绝不移除成员。 |

**踪迹到底证明了什么。** 因为 `id` 是内容地址，一条 `parent` 链接命名的是*那份确切的内容*，
事后无法重新指向别处；又因为事实不可变、删除是显式的（§10.1），祖先无法被悄悄改写。

> 因此踪迹证明的是**那个被命名的祖先曾经存在且未被改动**。它**不**证明这个孩子真的是由它
> 引起的 —— 任何人都可以把 `parent` 指向任何东西。这里的溯源是防篡改的，不是被证实的。

v2.0 称这是「无需任何人背书就能跨组织边界成立的溯源」。这说得太满了：*链接*无需背书即成立；
*出自何处的声称*是孩子作者的断言，值多少钱完全取决于那个作者值多少钱（§8.3、§10.1）。

### 8.3 该不该信它 —— 信任

折叠引用 `F` 的 `_.vote` 事实。读者 MUST 忽略自投票（`vote.author == F.author`），并 MUST
只计每个作者按 `seq` 的**最新**一票，这样改变主意的投票者永远不会被重复计数。

```
trust(F, quorum):                        # quorum 是**读者**的策略；MUST ≥ 1，默认 2
  若 F ∉ prefix                → 读者 MUST NOT 返回结果
  若 retracted(F)（§10.1）     → retracted     # 作者收回了它
  若 isSuperseded(F)（§8.1）   → superseded    # 新鲜度压过信心
  V ← 对每个作者 a ≠ F.author，取该作者在 F 上 seq 最大的 _.vote，
      且其 payload.verdict ∈ {corroborate, contradict}，
      且它没有被它自己的作者撤回
  C ← |{ v ∈ V : verdict == corroborate }|
  X ← |{ v ∈ V : verdict == contradict }|
  若 X ≥ quorum   → refuted
  若 X > 0        → contested
  若 C ≥ quorum   → consensus
  若 C > 0        → corroborated
  否则            → asserted
```

**状态优先级。** `retracted` > `superseded` > `refuted` > `contested` > `consensus` >
`corroborated` > `asserted`。前两个是事实自身的属性，其余是计票结果。

**这里堵上了 v2.0 的三个缺陷。** `retracted` 是一个独立状态：§10.1 要求信任区分「撤回」
与「替换」，而 v2.0 根本没有对应的状态，于是一条被撤回的事实可以折叠成 `consensus`。
`quorum` MUST ≥ 1，因为 `quorum = 0` 会让每一条没人投票的事实变成 `refuted`。而一张
`verdict` 缺失或无法识别的票会被**整条排除在 `V` 之外**，而不是占住它作者的名额 —— 在
v2.0 里，一张后来的垃圾票会静默地抵消该作者早先那张有效票。

要判断自投票，读者需要 `F` 本身。如果 `F` 不在前缀里，读者 MUST NOT 返回 `trust` 结果；
这是 §8.0 的重述，也是「被过滤的窗口不可折叠」的原因。

**quorum 数的是不同的 `author` 字符串，所以信任值多少钱，完全取决于 `author` 值多少钱。**
`author` 是自断言的，本协议不认证它（§10.1）。在一个不认证写入方的部署上，一个写入方就能
在任意 quorum 下、向任意方向制造出任何信任状态，而上面那条禁止自投票的 MUST 什么也买不到
—— 换个字符串就绕过去了。这是唯一一条其结果取决于「作者是彼此不同的主体」的折叠（§8.4 的
定序结果不取决于此），而未认证总线上的读者 MUST 把 `asserted` 之上的每一个状态都视为未
经证实。

**信任没有全局价值，所以永远不要在它上面协调。** 因为 `quorum` 是读者自己选的，两个读者
可以正当地就 F 是 `refuted` 还是 `consensus` 各执一词，而总线不裁决。任何需要所有参与者
一致的决定 MUST 建立在 §8.4 上 —— 那是每个读者都算得一样的东西。信任用于建议与分诊，绝不
用于仲裁。信任也不传播：关心一条链是否有效的读者，自己去走 §8.2 并检查祖先。

### 8.4 谁对它负责 —— 所有权

这条折叠是上面三条的**推论**：对一条事实负责不过是世界状态的又一片，而因为世界是全序的，
答案没有歧义。它排在最后是因为它是被推导出来的，不是因为它可选 —— 它是完全规范性的，而且
这条日志最有用的那个意外就落在这里。

**Δ 是日志的属性，不是读者的属性。** 总线 MUST 发布 Δ（§7.5），每个读者 MUST 用发布的值
去折叠。替换成自己那份的读者是**不合规**的，§9.1 对它不成立。在 v2.0 里 Δ 是每个读者自己
的旋钮外加一个文档化的默认值，于是两个用不同 Δ 折叠同一条流的读者，不仅在「谁持有领取」上
分歧，甚至在「这件活到底 `resolved` 没有」上都分歧。

**Δ 对一条日志终身固定。** 总线 MUST 在日志创建时把 Δ 持久化地记在日志旁边，MUST 用记下的
值去服务一条已存在的日志，并 MUST 拒绝以另一个 Δ 服务它（M12）。只「发布」Δ 是不够的：
§8.4 的每一个结果都是 *(前缀, Δ)* 的函数，所以一台每次启动都从环境里取 Δ 的总线，在那个
环境第一次不一样的瞬间，就把这条日志承载过的每一次领取重新解释了一遍。受损的不只是仍然
存活的领取。下面这段前缀

```
seq 1  _.claim    author agent-a  recv 1000
seq 2  _.resolve  author agent-a  recv 1900
```

在 Δ = 3600 下折出 `resolved(agent-a)`，在 Δ = 60 下折出 `open`：在那条 resolve 自己的
`recv` 上领取已经失效，于是没有胜出者去采信它。没有追加任何事实，没有改写任何字节，一个
终态却被撤销了 —— 而这正是 §9.3 存在的意义所在。把 Δ 记进日志，是让「日志的属性」这句话
对字节为真而不只是对散文为真；也正是它让一条日志能被复制到副本、能从备份恢复，而不必把
自己的含义寄存在某个运维的记忆里。

```
ownership(F):                            # 引用 F 的事实，按 seq 升序
  active ← []                            # 存活领取：{author, seq, recv}
  for fact in [ x ∈ prefix : x 带一个命名 F 的生命周期 ref ]，按 seq 升序：

    若 fact 是 F 上的 _.tombstone 且作者为 F.author  → 返回 dead        # 终态 §10.1

    active ← [ c ∈ active : fact.recv ≤ c.recv + Δ ]     # 确定性过期，以 recv 为锚

    若   fact.refs.claim_of   == F.id：
         active.push(fact)
    否则若 fact.refs.release_of == F.id 且 fact.author ∈ active 的作者集合：
         从 active 中移除 fact.author
    否则若 fact.refs.resolves   == F.id：
         owner ← active 中 seq 最小者的作者，若 active 为空则为 null
         若 owner != null 且 fact.author == owner  → 返回 resolved(owner)   # 终态
         # 否则不予采纳：只有当前领取胜出者可以 resolve。

  active ← [ c ∈ active : now ≤ c.recv + Δ ]              # 仅尾随过期
  返回 active 非空 ? claimed(active 中 seq 最小者的作者) : open

claimWinner(F) = ownership(F) 为 claimed 或 resolved 时的 owner，否则为 null
```

一条事实最多带一个生命周期 ref（§5.4），所以这些分支在构造上互斥，这条 `elif` 链是精确的。

**状态转移。** 状态为 `open`、`claimed(a)`、`resolved(a)`、`dead`。按上面的折叠读入一条
事实 `x`：

| 从 | 事实 | 条件 | 到 |
|---|---|---|---|
| `open` | `claim_of: F` | —— | `claimed(x.author)` |
| `open` | `resolves: F` | —— | `open`（忽略 —— 没有胜出者可采纳） |
| `open` | `release_of: F` | —— | `open`（忽略 —— 没有持有的领取） |
| `claimed(a)` | `claim_of: F` | `x.recv > claim(a).recv + Δ` | `claimed(x.author)` |
| `claimed(a)` | `claim_of: F` | 其他 | `claimed(a)` —— a 仍是 seq 最小的存活者 |
| `claimed(a)` | `release_of: F`，`x.author == a` | —— | `claimed(下一个存活者)` 或 `open` |
| `claimed(a)` | `release_of: F`，`x.author ≠ a` | —— | `claimed(a)`（忽略） |
| `claimed(a)` | `resolves: F`，`x.author == a` | —— | **`resolved(a)` —— 终态** |
| `claimed(a)` | `resolves: F`，`x.author ≠ a` | —— | `claimed(a)`（忽略） |
| `claimed(a)` | —— | `now > claim(a).recv + Δ`，且其后无事实 | `open` *（仅供参考）* |
| 任意 | `_.tombstone`，`x.author == F.author` | —— | **`dead` —— 终态** |
| 任意 | `_.tombstone`，`x.author ≠ F.author` | —— | 不变（忽略） |
| `resolved` / `dead` | 任何东西 | —— | 不变 —— **终态** |

**不变式。**

| # | 不变式 |
|---|---|
| I10 | 在任一前缀上，至多一个作者是领取胜出者。 |
| I11 | `resolved` 与 `dead` 是吸收态：一旦在前缀 P 上到达，对每个 P′ ⊇ P，`ownership` 返回同一个值（§9.3）。 |
| I12 | 胜出者是存活领取上的 `min seq` —— 有限集合上的全序，所以只要集合非空，最小元存在且唯一。 |
| I13 | 每一次状态转移恰由一条事实引起，唯一的例外是那条仅供参考的尾随过期，它由时钟引起。 |

**互斥结果。** 若多个作者追加了 `claim_of: F`，`seq` **最小**的那个胜出。每个读者从同一条
有序、带 `recv` 的前缀算出同一个赢家 —— 陈述与证明见 §9.1。

**为什么过期以 `recv` 为锚。** 一条领取在时间被证明已越过 `claim.recv + Δ` 时超时。只要
存在一条更晚的事实，这个证明就是那条事实自己的总线盖章 `recv` —— 对每个读者都相同，所以
折叠是确定的。只有一条其后没有任何事实的*尾随*领取才退回到墙上时钟 `now`。

> **尾随分支仅供参考，MUST NOT 被用作终局判定的依据。** 它可以把 `claimed(a)` 变成
> `open`，而在多条领取尾随时，它还能改变报出的是*哪一个*作者。它的存在是为了回答「新的
> 领取者该不该试一下？」，而需要稳定答案的读者应当等下一条事实 —— 那条事实会一次性为所有
> 人定案。v2.0 声称这个分支「只影响那个仅供参考的提示」；这对状态成立，对 owner 不成立。

**要 resolve，先 claim。** `resolves: F` **只**从 F 当前的领取胜出者那里被采纳。没有未
门控的路径。

v2.0 有一条：一条从未被领取过的事实可以被任何作者 resolve，作为「广播事实人人可关」的便利。
那个便利是一个拒绝服务原语。`resolved` 是终态，所以任何写入方一条格式良好的事实就能永久
关闭任何从未被领取的条目，而折叠无法把它与一次真实完成区分开。与此同时，实现还把这个分支
放得更宽 —— 只要没有*存活*的领取，就采纳陌生人的 resolve —— 于是一条已失效的领取（那意味
着这件活需要重新分派）可以被一个路人关掉。

要求先领取，代价是一次追加，而它恰好说出了正确的东西：*我对这件事负责*。它还简化了折叠 ——
不需要携带一个「到底有没有被领过」的标志位，而一条 `resolved` 事实总是点名那个解决了它的
作者，v2.0 则在它恰恰祝福的那个分支上返回 `resolved(null)`、把解决者的身份丢掉了。

### 8.5 可选约定

这些是叠加在同一本原之上的约定：没有新的 wire 机制，没有保留行为，对 §8.1–§8.4 和 §A 的
向量都没有影响。实现 MAY 完全忽略它们，读者 MUST NOT 把它们的缺席当作错误。它们的成熟度是
**实验性（Experimental）**：可能在小版本中变更或移除，§A.1 的一致性目标不包含它们。

**蚁群注册表（`sys.registry`）。** 智能体 MAY 声明自己消费什么、产出什么，好让主管能把两
者闭环。`payload.interests` 是一个针对事实类型的 §7.3 glob 数组；`payload.publishes` 是
一个类型数组。若存在，两者 MUST 是字符串数组；读者 MUST 忽略格式错误的条目。名册是每个作者
最新的一条 `sys.registry`，**排除其最新注册已被撤回的作者** —— 智能体就是这样离场的。名册
的排序由读者决定，且 MUST NOT 依赖 locale。两者各最多 64 条（§B）：每一条都是每个读者都要
求值的 glob。

把名册与流对折，能暴露出裸日志暴露不了的三个缺口：没有任何注册智能体感兴趣的事实类型（发向
虚空）、匹配不到任何东西的已声明兴趣（一个在等待寂静的智能体），以及从未出现过的已声明产出
（一个沉默的生产者）。保留命名空间的类型被排除在这项分析之外 —— 没有人会声明自己对一条
`_.claim` 感兴趣。

**上下文充分性（`context.requested` / `context.provided`）。** 一条事实可能断言「X 坏了」，
却不足以让关心它的智能体据以行动。为了不让这条路静默地断掉，感兴趣的智能体追加一条
`context.requested`，用 `refs.about` 点名那条单薄的事实，并带上 `payload.question`；任何
能回答的智能体追加一条 `context.provided`，用 `refs.answers`（或 `refs.parent`）点名那个
请求。读者折出仍在等待回答的请求。

---
## 9. 性质与证明

四条结果撑起这个设计。每条都暴露前提、从 §8 证明，并且 —— 实践中最要紧的部分 —— 标明边界，
因为一条没有写下边界的保证，一定会在边界之外被依赖。

全文中 P 表示一个完整前缀（§8.0），P′ ⊇ P 表示一个更长的前缀。

### 9.1 互斥性

> **定理 1。** 设 F 是一条事实，P 是一个完整前缀，其中至少存在一条 F 上的存活领取。那么
> 在 P 上的 `ownership(F)` 恰好指定一个作者为领取胜出者，并且每一个持有 P 且用同一个 Δ
> 折叠的读者，指定的是*同一个*作者。

**证明。**

1. 由 §5.8，`seq` 是由单一组件赋予、每份内容恰好一个的严格递增整数。因此 P 中的事实只
   容许一种 seq 升序枚举 —— §8.4 规定的迭代顺序是唯一的，不是一个选择。
2. 由 §3.2 事实不可变，由 §4.2，§8.4 读取的那些字段 —— `refs`、`author`、`recv`、`seq`
   —— 在追加时即已固定。因此持有 P 的两个读者对每一个分支条件读到的值完全相同。
3. §8.4 循环的每一步都是（当前 `active` 集合、正在读入的事实、Δ）的全函数。对 (1) 的枚举
   做归纳，读入 k 条事实之后的 `active` 集合只是 P 与 Δ 的函数。
4. 循环结束时，胜出者是 `active` 上的 `min seq`。`seq` 是全序 (1) 且 `active` 有限，因此
   只要 `active` 非空，最小元存在且唯一。
5. 由 (3) 与 (4)，被指定的作者是 (P, Δ) 的函数。P 与 Δ 相同的两个读者得到相同的值。∎

**前提，以及去掉每一条会怎样。**

| 前提 | 出处 | 去掉它则 |
|---|---|---|
| 单一全序 | §2.4、§5.8 | 两条领取可能不可比；`min seq` 无定义；不存在可供达成一致的赢家 |
| 不可变性 | §3.2 | 折叠不是日志的函数，因为同一个 P 在不同时刻给出不同结果 |
| 完整前缀 | §8.0 | 缺了那条更小 seq 领取的读者会选出较大的那个 —— **两个读者都以为自己赢了** |
| 所有读者共用一个 Δ | §8.4、§B | 读者对哪些领取存活分歧，因而对赢家分歧，也对这件活到底 `resolved` 没有分歧 |
| 过期以 `recv` 为锚而非 `ts` | §5.6/§5.7 | 过期建立在一个作者提供的数字上：把 `ts` 设成很久以前，自己的领取立刻过期、绕开互斥；设成未来则永久霸占 |
| 折叠的纯粹性 | §8.0 | 两个读者可以从完全相同的输入算出不同答案 —— 而那正是失败的定义 |

**这条定理**没有**说什么。**

- 它没有说胜出者*有权*做这件活。它说的是每个读者都同意胜出者是谁。如果 `author` 被伪造
  （§10.1），大家一致同意的赢家可能是个冒名者 —— 而每个读者都会一致同意那个冒名者。
  **互斥是「达成一致」，不是「授权」**，而这也是 §8 里唯一在 `author` 不诚实时依然完好的
  结果 —— 因为它从不追问一个名字是否属实，只问哪个名字排在前面。
- 它不延伸到尾随分支。见 §9.3。
- 它不是操作系统意义上的互斥原语：没有任何东西阻止一个落败的作者照样去做那件活。它保证的
  是：如果每个参与者都在行动前折叠，那么恰好有一个会得出「我该行动」的结论。

### 9.2 确定性

> **定理 2。** 对 §8.1–§8.4 中的每一条折叠，持有同一个完整前缀 P 且使用同一组读者参数的
> 两个读者，返回完全相同的结果。

**证明。** 每条折叠都被定义为 P 与其参数的纯函数：没有折叠读时钟（§8.4 的尾随分支除外，
这里由参数排除在外），没有折叠读外部状态，也没有折叠读可变字段 —— 因为根本不存在可变字段
（§4.2）。由定理 1 的第 (1) 步，P 确定唯一的有序序列。同一个函数、同一份输入、同一个输出。∎

**读者参数恰好只有两个**，把它们点名出来，是为了让「同一组参数」成为可核对的而不是含糊的：

| 参数 | 谁选 | 不一致的后果 |
|---|---|---|
| Δ | **日志**（§8.4）—— 读者 MUST NOT 自选 | 读者对所有权分歧；§9.1 作废 |
| `quorum` | **读者**（§8.3）—— 正当地因读者而异 | 读者正当地对信任分歧，这正是任何东西都不得在它上面协调的原因 |

**边界 —— 前缀不同不是分歧。** 处在不同长度前缀上的两个读者可以返回不同答案。那是延迟，
而且是预期之中的：一个比另一个看到了更多世界。分歧指的是处在**同一个** N 上的两个读者给
出不同答案，而定理 2 说那不可能发生。它的实践形式是：需要稳定答案的读者应当等下一条事实，
而不是等一个超时 —— 因为一条事实一次性为所有人定案，而一个超时只为一个读者定案。

### 9.3 单调性

这是共享世界真正需要的性质，也是 v2.0 从未陈述过的那条：**如果一个结论会悄悄反转，就没有
智能体能据以行动。**

诚实的答案是：有些结果稳定，有些不稳定，而这个设计依赖于精确地知道是哪些。

> **定理 3（吸收性）。** 在 Δ 固定的前提下，若 P 上的 `ownership(F)` 返回 `resolved(a)`
> 或 `dead`，则对每个 P′ ⊇ P，它返回同一个值。

**证明。** 这两个值都由循环内部的一次立即返回产生 —— 来自当前胜出者的一条 `resolves`，或
来自 F 的作者的一条 `_.tombstone`，记为事实 x。在 P′ 中枚举仍按 seq 升序（定理 1 第 1 步），
且 x 仍占据同一个 `seq`，因为 `seq` 从不复用（§5.8）。P′ 相对 P 新增的每一条事实，`seq`
都高于 P 的最大值，因而高于 x。于是循环在读到 x 时所读过的前序事实完全相同，且 Δ 未变，
故在 x 处算出同一个 `active` 集合，因而在读到任何新东西之前返回。∎

**这个前提是承重的。** 定理 2 已经把确定性表述为 *(P, Δ)* 的函数；吸收性通过返回分支所查
的那个 `active` 集合继承了同一个参数。Δ 一变，`resolved` 就可能变回 `open`（§8.4）。这正是
§8.4 要求把 Δ 记进日志、并对一条日志终身固定的原因：智能体据以行动的正是吸收性，而一个能
在它脚下被换掉的参数不是参数，是一次分叉。

**什么是单调的。** 随着前缀增长，下列结果只增不撤：

| 结果 | 原因 |
|---|---|
| `resolved(a)`、`dead` | 定理 3 |
| `history(S)` | 成员资格是逐事实的谓词；加事实只会加成员（I2） |
| `descendants(F)` | 同上（I9） |
| `chain(F)` | 只通过把缺口标记解析成事实并继续向上而增长（I9） |
| 「事实 F 存在」 | 只追加（§3.2） |

**什么不是单调的 —— 而且是有意的。**

| 结果 | 怎么变 | 为什么这是对的 |
|---|---|---|
| `current(S)` | 移到更晚的成员；头部被撤回时变为 `null` | 它回答的是「X *现在*是什么」。一个不会变的寄存器就不是寄存器。 |
| `supersededBy(F)` | 后继到来时 `null` → 某个 id；该后继自己被撤回时退回 `null` 或退到更晚的候选 | §8.1 规则 5：否则撤回一个糟糕的替换会让原事实搁浅 |
| `trust(F, q)` | 任意状态可变为任意状态 | 它是意见计票，而意见会变。这正是 §8.3 禁止在它上面协调的原因。 |
| `claimed(a)` | 过期时变为 `claimed(b)`，释放时变为 `open` | 这**就是**崩溃恢复。一个永远不会移动的领取，会让一个崩掉的智能体永久搁置一件活。 |

**把边界说锋利。** `claimed(a)` *不*稳定，而 §8.4 的尾随分支让情况更糟：在没有更晚事实来
证明时间已经推进时，折叠退回读者自己的时钟，于是处在**同一前缀**上的两个读者可以给出不同
答案。这是定理 2 唯一够不到的地方，也是 §8.4 把该分支标为仅供参考的原因：

> 读者 MUST NOT 基于一个尾随的 `claimed` 或尾随的 `open` 做终局判定。它 MAY 用它来回答
> 「我该不该试？」—— 在那里判断错了的代价是一次白跑的领取，而折叠随后会把它理清。

让这在实践中安全的规则是：**在吸收态上行动，在参考态上试探。** 承诺去做一件活以「成为胜出
者」为闸门（§7.4 第 5 步），而完成被记录为一条事实，那是吸收的。所以一次错误的参考性读取
最坏的后果是一次多余的领取，绝不会是一次丢失或重复的完成。

### 9.4 幂等性

> **定理 4。** 追加一条其作者域字段与既有事实逐字节相同的事实，日志保持不变，并返回既有
> 的回执。

**证明。** 由 §5.9，`id` 只是作者域的函数。因此相同的作者域产生相同的 `id`。§7.2 第 3 步
在赋予 `seq` 之前查 `id → seq` 索引；命中时返回存储的 `{seq, recv, sig}` 且不写任何东西。
日志未变，而响应标识的是同一条事实。∎

**为什么这是正确的默认。** 一个在追加途中断线的客户端不知道那条事实有没有落地。有了定理 4
它只需重试，两种情况下结果都一样 —— 不用发明去重键，不用推敲「至少一次」的注意事项，不用
写补偿逻辑。

**以及为什么它需要一个逃生口。** 同一条定理也意味着一次*正当的重复*会静默塌缩：释放之后
重新领取 F，或者在有了新证据之后再投一次同样的票，会产生相同的内容、因而是同一条事实 ——
于是本该发生的新动作没有发生，而作者被告知追加成功了。`nonce` 是作者主动改变作者域的手段
（§5.5），而带生命周期 ref 或 `vote` 的事实 SHOULD 总是带一个。

这正是 git 这类内容寻址存储的模型：相同的内容就是同一个对象；想要另一个对象，就改内容。

---

## 10. 授权与强制

这里把两套不同的机制放在一起，因为实现者经常混淆它们：

- **折叠门控（§10.1）** 是*读者*施加的规则。它们决定一条事实*意味着什么*。总线不求值它们，
  也 MUST NOT 因为一条事实过不了某个门控而拒绝追加。
- **总线规则（§10.2、§10.3）** 是*总线*在追加时施加的检查。它们决定什么可以被*存储*。它们
  关乎格式合法性与资源上界，从不关乎真伪或权限。

本协议中没有任何东西限制*谁可以追加*。只要字段值域允许，任何写入方都可以写任何东西。

### 10.1 门控表

**折叠门控**是读者 MUST 施加的规则。未加门控的键是*自断言*的：它说的是它的作者所相信的
东西，由读者自行掂量。

| ref | 读者 MUST 施加的门控 |
|---|---|
| `resolves` | 只采纳来自目标当前领取胜出者的。没有未门控的路径 —— 要 resolve，先 claim（§8.4）。 |
| `release_of` | 只采纳来自在目标上持有存活领取的作者的。 |
| `tombstones` | 只在 `fact.author == target.author` 时采纳。任何其他作者的墓碑 MUST NOT 撤回目标；读者 MAY 把它呈现为一次*被请求的*撤回。 |
| `vote` | 当 `vote.author == target.author` 时忽略（§8.3）。 |
| `claim_of` | 不按身份门控 —— 按**序**门控。seq 最小的存活者胜出（§8.4）。 |
| `supersedes` | 只在 `fact.author == target.author` 时采纳（§8.1）。第三方表达陈旧的方式是反驳（§8.3）或往寄存器里写，而不是把别人的陈述退役。 |
| `parent`、`subject`、`about`、`answers` | 不门控。自断言。 |

**自断言的链接是声称，不是证明。** `parent` 说的是作者相信这由那引起；`subject` 说的是作者
相信这是关于 X 的一句陈述。两者都没有被任何东西验证，而把它们当作已被证实的读者，是读错了
信任边界（§2.2）。内容寻址买到的东西更窄，但仍然有价值：链接的*目标*事后无法被掉包（§8.2）。

**删除是一条事实。** 总线从不修改或静默丢弃一条已存储的事实。删除是追加一条 `_.tombstone`，
其 `refs.tombstones` 点名目标，并受上面那条门控约束。

打墓碑与取代是**不同**的，折叠 MUST 把它们区分开：

- **被取代（superseded）** —— 一个后继替换了这条。寄存器往前走（§8.1）；信任报告
  `superseded`（§8.3）。
- **被撤回（retracted）** —— 作者把这条收回。寄存器折叠为 `null` 而不是折回上一个值；信任
  报告 `retracted`；生命周期是 `dead` 且是终态。

墓碑门控是 v3.0 新增的，它堵住了一个真实的洞。在 v2.0 里，任何作者都可以给任何事实打墓碑，
而其效果异常具有破坏性：目标的生命周期变成 `dead` —— 一个**终**态 —— 它的寄存器折叠为
`null`，而压缩随即有权销毁它在磁盘上的 payload（§11.2）。这是一个协议授权的、每个写入方都
能用的数据销毁原语。撤回现在恢复了它本该的样子：**把你自己的陈述收回去。**

运维驱动的删除（法律下架、擦除请求）刻意**不**被建模成事实。它是对日志的带外操作，受 §11.2
约束。

**`author` 是自断言的。** 它是写入方自己选的字符串。本协议不认证它，也没有身份的概念。认证
是传输层的事 —— mTLS、网关、API key 头 —— 而部署 MAY 把总线跑在可信网络上，或者跑在一个
校验或盖章 `author` 的代理后面。

> 一个能被不可信方触达的总线，对 `author` 没有任何完整性叙事，因而对 §8.3、以及 §8.4 中
> 「授权」的那一半也没有。公开部署 SHOULD 认证写入方，SHOULD NOT 在未认证的情况下暴露写
> 权限。

折叠层里没有任何东西能弥补这一点，读者也 SHOULD NOT 假装可以：§8 中的每一条保证都相对于
`author` 的诚实而言，唯有 §9.1 的定序结果例外 —— 那一条无论如何都成立。

### 10.2 强制规则（总线 MUST）

刻意最小化：足以防止一条只追加日志被武器化成无界增长、无界计算，或者没有读者能折叠的乱码。
其余一切都是读者的事。

| # | 规则 | 违反时 |
|---|---|---|
| M1 | 拒绝违反任何 §5 必需性、类型或值域规则的事实 | `400` |
| M2 | 拒绝携带多于一个生命周期 ref 的事实（§5.4） | `400` |
| M3 | 拒绝超出 §B 上限的事实 | `413` |
| M4 | 复算 `id`；拒绝客户端提供的不匹配值 | `400` |
| M5 | 拒绝因果深度超过上限的事实（§B） | `400` |
| M6 | 赋予稠密、严格递增、永不复用的 `seq`（§5.8） | —— |
| M7 | 赋予随 `seq` 单调不减的 `recv`（§5.7） | —— |
| M8 | 对 `id\|author\|type\|ts\|recv\|seq` 签名（§5.10） | —— |
| M9 | 在返回 `201` 之前按持久化策略落盘（§11.1） | —— |
| M10 | 恢复时复验 `id` 与 `sig` 并分别报告两个计数（§11.1） | —— |
| M11 | 日志内部（非尾部）损坏时拒绝启动（§11.1） | —— |
| M12 | 创建日志时把 Δ 记在日志旁；拒绝以另一个 Δ 服务它（§8.4） | —— |
| M13 | 接受未知 `refs` 键，且 MUST NOT 解释它们 | —— |
| M14 | 忽略且 MUST NOT 存储未知顶层字段（§5.3） | —— |
| M15 | 除 §11.2 下的压缩之外，绝不修改或删除已存储的事实 | —— |

**M12，精确表述。** Δ 不是一个运行时设置，它是日志*含义*的一部分（§8.4）。一台每次启动都从
环境里读 Δ 的总线，会在那个环境第一次不同的时候 —— 换了容器镜像的默认值、恢复了一份备份、
换了个运维、起了个副本 —— 悄悄把整段历史重新折一遍。记录下来的值是权威：没给 Δ 就启动的
总线采用日志的那个值，给了冲突 Δ 的总线 MUST 拒绝，而不是替人做选择。改一条活日志的 Δ 是
一次有意的破坏性操作，它该由运维显式做出，而不是由一次重启代劳。

**M5，精确表述。** 深度的计算方式是：沿 `refs.parent` 在**追加时刻已在日志中的事实**之间
行走，当新事实的 parent 缺席或未指定时记其深度为 1。深度恰好等于上限的事实被接受；
`depth > max` 被拒绝。

> **这限的是行走，不是链条。** `parent` MAY 命名一条不在场的事实（§8.2），所以作者可以从
> 叶子开始追加、让祖先随后到达，从而构造任意深的链。这是被允许的 —— 在一条按游标读取的
> 日志里，追加顺序不得决定有效性 —— 也就是说 M5 是对*追加时刻的计算量*的一个廉价上界，
> 而不是对读者日后会折叠的任何踪迹深度的保证。读者 MUST 自行限界（§8.0）。v2.0 把这条规则
> 说成一项安全保证；它是一个限流器。

**环。** 经由合规总线无法构造 `refs.parent` 的环：闭合一个 A→B→A 需要 A 的 `id` —— 因而
需要 A 已冻结的内容，而那份内容已经点名了 B —— 出现在 A 被哈希之前，也就是需要一次 sha256
原像。这个论证对被追加的事实成立，对其他一切都不成立：导出的、复制的、被截断的、手工修复过
的日志都存在。这正是 §8.0 无论如何都要求折叠终止的原因，也是总线自己那次深度行走 MUST 有
界的原因。

### 10.3 推荐规则（总线 SHOULD / MAY）

这些是部署选择。一个把它们全部略去的实现仍然合规；采纳了的实现 MUST 记录并暴露自己的设置
（§7.5），因为每一条都改变了一个运维承诺。

| # | 规则 | 说明 |
|---|---|---|
| R1 | 在任何能被不可信方触达的网络上 SHOULD 认证写入方 | §10.1、§12 |
| R2 | MAY 施加按作者的令牌桶与一个全局准入上限 | 拒绝是「事实没被写下」，绝不是状态变更。**不规定默认值**：给一个可选机制规定默认值，是协议兑现不了的承诺 |
| R3 | 除非有意暴露，SHOULD 绑定在回环接口上 | 总线信任它的调用者 |
| R4 | MUST 通过 `/info` 报告非稳定密钥 | 重启前写下的每个 `sig` 都变得无法验证（§5.10） |
| R5 | SHOULD 默认逐条 `fsync`；MAY 提供更宽松的策略 | 宽松策略改变了 `201` 的含义，MUST 被报告（§11.1） |
| R6 | SHOULD 对 `sig` 消息的字段做长度前缀 | §5.10 的分隔符歧义；未来版本会要求 |
| R7 | MAY 把过大的读取 `limit` 夹紧而不是拒绝 | §7.3 |

---

## 11. 存储与恢复

### 11.1 日志

总线是一条只追加日志 —— 每行一条 JSON 记录 —— 按 `seq` 顺序写入。`recv` MUST 随 `seq`
单调不减。

**持久性。** `201` 意味着该事实已按配置的策略落盘。默认是逐条 `fsync`（R5）。更宽松的策略
（按批、按秒）MAY 被提供，且 MUST 通过 `/info` 报告，因为它改变了 `201` 所承诺的东西。

**恢复。** 按顺序读日志。

- 遇到**撕裂的最后一条记录** —— 一段解析不了的尾部字节 —— 总线 MUST 在接受任何追加之前
  **把文件截断到最后一个能解析的字节偏移**。跳过这个碎片、在它后面继续追加是不够的：新记录
  会被拼接到碎片上，合起来的那一行永远解析不了，于是一条已确认的事实在下次重启时被静默丢失，
  而它的 `seq` 已经被发给了另一份内容。这会破坏 §8 全部内容所依赖的那个全序，也是本版本
  修复的最严重缺陷。
- 遇到**只丢了换行符的完整尾部记录**，总线 MAY 通过补写终止符来修复文件，而不是截断。不需要
  丢掉任何东西就能让文件重新可追加。
- 遇到**不是最后一条的损坏记录**，总线 MUST NOT 正常启动（M11）。它 MUST 报告偏移量并要求
  一次显式的修复动作。静默跳过一条内部记录并不会重编号，但会移除一条其他读者已经折叠过的
  事实，从而永久地把它们的视图劈成两半。
- 总线 MUST 在恢复时复验 `id == sha256(JCS(record))`，并 MUST 报告失败计数（§7.5）。§5.10
  的 `sig` 不覆盖内容，所以这是唯一能发现磁盘上 payload 被篡改的检查。
- 密钥稳定时总线 MUST 验证 `sig`，并 MUST 与 `id` 的失败分开报告。它们含义不同：`sig` 失败
  是被篡改或用了别的密钥的**头部**；`id` 失败是被篡改的**内容**。
- `seq` 恢复为出现过的**最大** `seq`，下一次追加取它之后的那个。

  > **「永不复用 `seq`」在这里能说什么、不能说什么。** 对日志仍然持有的每一条事实，这条
  > 规则是绝对的：已存储的 `seq` 绝不会被交给别的内容（M6、M15）。被上面那次截断抹掉的
  > `seq` 是另一回事，而且它和持久化策略互相牵连：
  >
  > - 逐条 `fsync` 时，撕裂的记录从未被确认过 —— 崩溃发生在写入与 `201` 之间 —— 所以没有
  >   任何客户端持有它的回执，重新发放那个号码对所有人都不可见。
  > - 放宽策略下，`201` 可能已经为一段随后被撕掉的内容返回过。重新发放那个号码，就是把一个
  >   `seq` 在两个读者眼里交给了两段不同的内容，而这恰恰是 §9 的证明要排除的分叉。
  >
  > 总线 MUST 报告这次截断（§7.5），让它可见而不是靠推断。需要绝对规则的部署 MUST 采用逐条
  > `fsync`；在放宽策略下，「`201` 可能被一次崩溃撤销」延伸到它命名的那个 `seq`，而这正是
  > 放宽策略所买到的东西的一部分。

- Δ MUST 从日志自己的记录里读（§8.4），而不是从环境里读；当记录的 Δ 与启动时给定的 Δ 不同，
  总线 MUST 拒绝服务这条日志（M12）。

没有内存里的状态机需要重建：日志*就是*状态，而派生索引（`seq` 计数器、`id → seq` 映射）是
纯投影。这就是 §1.3 第 5 步的可靠性红利。

### 11.2 压缩

压缩用来回收空间。它只有一条约束性规则：

> **压缩 MUST NOT 改变任何 §8 折叠的结果。**

具体地说，压缩器：

- MUST 保留每条事实完整的骨架 `{id, seq, recv, author, refs, sig}`。每条折叠都依赖它：踪迹
  需要 `refs.parent`，领取胜出者需要 `seq` + `author` + `refs.claim_of`，信任需要 `author`
  + `refs.vote`。
- MUST NOT 丢弃任何其 payload 会被折叠读取的事实的 `payload`。这至少包括每一条 `_.vote`
  （§8.3 读 `payload.verdict`）和每一条被 `current(S)` 返回的事实。
- MUST NOT 把「被取代」单独当作丢弃 payload 的依据。只有**撤回**（§10.1）才是。被取代的事实
  仍然是 `history(S)` 的成员，而 §8.1 明确支持读者在其上累积 —— v2.0 的压缩器把每个寄存器
  的每个非头部成员都剥了，恰好摧毁了寄存器那一节所推荐的用法。
- MUST 写入临时文件、fsync 它、原子重命名，并 fsync 所在目录。

**被压缩过的事实不再哈希到它自己的 `id`。** 这不可避免 —— 丢掉 payload 就改变了内容 ——
它意味着一条被剥过的事实，其内容地址是一个历史名字而不是一个活的校验和。因此实现 MUST
**标记**那些 payload 被丢弃的事实，好让读者能区分「已验证」与「无法验证」，而不是断定日志
被篡改了。读者 MUST NOT 把一条被压缩过的事实报为完整性失败。

有鉴于此，运维应当把压缩当作一项针对**已撤回内容**的留存策略，而不是一般性的空间优化。一条
必须进一步缩小的日志，应当在某个检查点截断并归档 —— 那是诚实地丢掉旧前缀，而不是破坏其上
的折叠。

### 11.3 复制与扩展

在 §2.4 之内被允许的：

- **只读副本**按 `seq` 顺序接收日志并提供读取。构造上就是可靠的：一个落后 N 条的副本就是一
  个处在更早前缀上的读者，而 §9.2 的边界已经覆盖了它。副本 MUST NOT 接受追加。共享总线密钥
  的副本可以验证 `sig`；不共享的不行。
- **按世界分片** —— 为互不相干的 subject 空间开独立日志 —— 就是几个独立的世界。一次折叠
  MUST NOT 跨两条日志：`seq` 只在一条日志内部有意义，跨日志根本没有序可折。

不被允许、本文也不描述的：任何让两个组件为同一条日志赋予 `seq` 的安排。那是另一个协议。

---

## 12. 安全性考量

### 12.1 一张表说清威胁模型

| 攻击者 | 能做什么 | 不能做什么 | 缓解 |
|---|---|---|---|
| **有网络访问的写入方** | 追加任何值域允许的东西；领取任何事实；写任何 `author` 字符串；灌爆日志 | 改动或删除已有事实；改 `seq` 或 `recv`；伪造 `sig` | R1（认证）、R2（准入限流）、R3（不要暴露） |
| **冒充他人 `author` 的写入方** | 制造任意 §8.3 信任状态；看起来像持有或释放了别人的领取；撤回或取代被冒充作者的事实 | 改变任何读者*在同一条日志上*算出的东西 —— 每个读者都会一致同意那同一个被伪造的赢家（§9.1） | R1。协议内没有缓解手段（§10.1） |
| **读者** | 读到日志上的一切 | 写；验证 `sig`（密钥对称，§5.10） | 作者之间不存在保密边界 —— 这是设计事实，不是待补的缺口 |
| **拥有文件系统访问权的人** | 改写 payload；拼接或删除记录；替换整条日志 | 做到不被发现：`id` 复验抓内容改动，`sig` 抓头部改动（§11.1） | M10、稳定密钥、文件系统权限 |
| **恶意总线** | 乱序、丢弃、捏造、伪造任何签名 | —— | **没有。** 总线是被信任的（§2.3）。一个无法信任自己总线的部署，用不了这个协议 |
| **资源攻击者** | 提交很深的因果链、超大 payload、昂贵的 glob | 越过 §B 的上限 | M3、M5、§B 的 glob 长度上限、R2 |

### 12.2 协议刻意不保护什么

- **保密性。** 日志上的一切对每个读者都可读。不能被共享的事实，就不要放到共享日志上。
- **`author` 的完整性。** §10.1。一切后果都从这一句流出，而没有传输层认证时 §8.3 作废。
- **payload 的真伪。** 总线从不判断内容（§2.2）。§8.3 是被设计出来的应对 —— 由其他作者反驳
  —— 而它仅供参考（§8.3）。
- **分区下的可用性。** §2.4。

### 12.3 运维要求

1. **设置稳定的 `sig` 密钥。** 每次启动自行铸一把的总线，无法验证上次重启之前写下的任何
   东西，而且 MUST 说明这一点（R4）。
2. **不要暴露一个未认证的总线。** 它信任它的调用者 —— 与一个未认证的 Redis 在同样的意义上、
   到同样的程度。
3. **把 `id_failures > 0` 当作一次事故。** 它意味着磁盘上的内容不再与它的地址匹配：篡改，
   或者存储损坏。两者都不是常态。
4. **为智能体派生的任何子进程做环境变量白名单。** 尤其是总线密钥 MUST NOT 被传下去；一个
   派生出来的 worker 需要的是追加，而追加不需要任何密钥。

---

## 13. 相关工作

这个协议是一堆已知想法的组装。新的是这个组合，以及「为什么别的都不需要」的论证。

| 来源 | 取了什么 | 留下了什么 |
|---|---|---|
| **黑板架构**（Hearsay-II、HASP） | 一块共享板面，各路专家把中间结果写上去、各自独立地读；没有专家被点名 | **控制组件** —— 那个决定下一个跑哪个专家的调度器。改由全序仲裁（§3.1） |
| **共识主动性 stigmergy**（Grassé） | 靠共享介质而不是靠消息来协调；介质持有状态 | 什么也没留下 —— 这是指导意象（§3.1） |
| **Linda 元组空间**（Gelernter） | 一个共享的、按内容关联寻址的空间；生产者与消费者从不点名彼此 | **破坏性的 `in`**。被取走的元组就没了，于是后到的读者无法重建世界，空间退化成一个工作队列。这里没有东西被消费；所有权是一次折叠，不是一次移除（§8.4） |
| **事件溯源 / CQRS** | 日志是唯一真相；每个状态都是投影 | 按聚合根划界，以及「只服务一个应用的 schema」这个假设 |
| **Kafka** | 一个有序、持久、按游标读取的分区 | 把消费者组与 offset 当作*协议* —— 那些是投递语义，而投递意味着收件人（§1.3 第 2 步）。单个带签名的分区是与这条总线最接近的类比 |
| **Git** | 内容寻址、不可变、只追加历史、按游标获取；相同内容就是同一个对象 | 合并模型。这里只有一个序，没有什么可合 |
| **Lamport 时钟 / 全序广播** | 互斥作为「序」的后果而不是「锁」的后果 | 对序本身的分布式共识。由一条总线赋予它（§2.4） |
| **CRDT** | 无需协调即可收敛 | 代价：限制你能*说*什么，直到每个操作都可交换。分歧在这里是数据，不是待消解的冲突（§1.2） |
| **CAN 总线** | 内容寻址的广播加本地过滤；没有节点被寻址 | 那套实时仲裁机制 |
| **RFC 8785（JCS）** | 规范化 JSON，让「同一条事实」在每种语言里都意味着同样的字节 | 什么也没留下（§5.9） |
| **科学方法** | 可被质疑的陈述，被佐证或被反驳，没有中央真理裁判 | 对收敛的期待。§8.3 明确不收敛 |

最接近的单一先祖是黑板，而这个协议诚实的一句话总结是：**一块给自治智能体用的黑板，控制组件
被全序取代，而每个专家都可以永远读到整块板。**

---

## 附录 A. 一致性

### A.1 一致性目标

有三个，实现 MUST 声明自己主张的是哪些。

**合规总线**实现 §2.1 的四项职责、§5 的校验、§7.1–§7.3、§5.9、§5.10、§10.2 与 §11。它 MAY
忽略每一个读取过滤器（§7.3）。它 MUST NOT 提供任何会改变折叠结果的运维端点（§7.5）。

**合规读者**在完整前缀上、用总线发布的 Δ，精确实现 §8.1–§8.4 的折叠。§8.5 是可选且实验性的。

**合规客户端**遵守撰写规则：字段值域（§5）、每条事实一个生命周期 ref（§5.4）、可重复的关系型
事实带 `nonce`（§9.4），以及任何意图成为寄存器当前值的事实带 `refs.subject`（§8.1）。

### A.2 向量集

本协议附带一套规范的跨语言一致性向量（`conformance/vectors.json`）。**它是互操作契约；散文
不是。** 一套向量集 MUST 至少钉住：

- **§5.9** —— 对每个向量，精确的 JCS 规范串与得到的 `id`。覆盖 MUST 包括：嵌套键排序；一个
  非 BMP 键（UTF-16 与码点排序的陷阱）；`1e-7`、`1e-6`、`1e16`、`1e21` 处的数字，以及一个
  超过 2⁵³ 的整数；一个整数 `ts`；`ts` 为 `-0`；需要转义的 unicode 字符串；一个孤立代理项；
  `refs` 缺席 vs 为空；`nonce` 存在 vs 缺席。
- **§8 的每一条规范性折叠** —— `history`、`current`、`supersededBy`、`isSuperseded`、
  `chain`、`descendants`、`trust`、`ownership`、`claimWinner`。一条被宣布为规范性却没有向量
  的折叠，在实践中不属于契约。
- **两种读法会分岔的用例**，这才是向量的*用途*：一个至少四个成员的 subject 分组（用来区分
  「直接后继」与「最新」）；一条事实的两个显式后继；寄存器头部被撤回；非头部成员被撤回；一条
  恰好在 Δ 边界上过期的领取以及一条越过一秒的；一条领取失效后来自陌生人的 resolve；对一条
  从未被领取过的事实的 resolve；非持有者的 release；一条祖先悬空的链（缺口标记）；分叉上的
  `descendants`；一张 verdict 无法识别的票；一张自投票；一条被撤回事实的 `trust`；
  `quorum = 1`。

跨语言校验器 MUST 检查折叠输出，而不只是哈希。一个复现了每一个 `id` 却不检查任何折叠结果的
校验器，对 §8 没有提供任何证据 —— 而全部含义都在 §8 里。

改动一个已提交的向量是**破坏 wire 的变更**：它 MUST 是有意的、逐个哈希审阅过的，并在做出该
变更的提交里声明。而这条规则有用的另一半是反过来的：**一次只重述规范的变更，必须让每一个
向量逐字节不变。** 一次「纯重写」之后向量动了，说明这次重写改变了语义。

---

## 附录 B. 参数与默认值

| 参数 | 默认值 | 谁设定 | 说明 |
|---|---|---|---|
| **Δ —— 领取超时** | 600 秒 | **日志** | §8.4。经 `/info` 发布；读者 MUST 使用发布的值，MUST NOT 覆盖它 |
| 信任 quorum | 2 | 读者 | §8.3。MUST ≥ 1 |
| 因果深度上限 | 64 | 运维 | §10.2（M5）。限的是追加时刻的计算量，不是踪迹深度 |
| 读取 `limit` 默认值 / 上限 | 100 / 10 000 | 运维 | §7.3 |
| `payload` 上限（序列化后） | 1 MiB | 运维 | §5.3，`413` |
| `refs` 键数上限 | 64 | 运维 | §5.4 |
| `type` / `author` / `subject` 上限 | 各 256 字节 | 运维 | §5.1、§5.2、§5.4 |
| `nonce` 上限 | 128 字节 | 运维 | §5.5 |
| glob 模式上限 | 256 字节 | 运维 | §7.3。以 `400` 拒绝；该模式由攻击者提供 |
| `interests` / `publishes` 条数上限 | 各 64 | 运维 | §8.5。每一条都是每个读者都要求值的 glob |
| 持久性 | 逐条 fsync | 运维 | §11.1。更宽松的策略 MAY 被提供且 MUST 被报告，因为它改变了 `201` 的含义 |
| 准入速率 | 无 | 运维 | §10.3（R2）。协议不规定默认值 |

总线 MUST 暴露它生效的取值（§7.5）。注意可信核心里能配置的东西有多少 —— 这也是 §1.3 第 5 步
的又一个推论。Δ 是唯一一个既不属于读者、也不纯属运维的参数：它是这条日志含义的一部分，必须
对每个读它的人都相同。

---

## 附录 C. 版本与兼容性

### C.1 立场

**v3.0 与 v2.0 不是 wire 兼容的**，并且不提供迁移路径。仅规范化这一项改动就重新寻址了每一条
事实，所以一条 v2.0 日志被 v3.0 实现读取时，每一条记录的 `id` 验证都会失败。

被支持的过渡方式是**开一条新日志**。必须保留的 v2.0 日志应当归档，并用 v2.0 的读者去读。对
一个尚无外部实现的 alpha 阶段协议来说，这是一个有意的选择：一层兼容垫片会把 §C.2 列为破坏性
变更的那些缺陷原样保留下来。

### C.2 相对 v2.0 的变更

| 变更 | 章节 | 类型 |
|---|---|---|
| 推导链改以「共享世界状态」为本原重建；删去 v1 迁移材料 | §1 | 编辑性 |
| 文档按规范体例重构：系统模型、术语表、逐字段规范、完整实例、操作时序、证明、安全性考量 | §2、§3.3、§5、§6、§7、§9、§12 | 编辑性 |
| 折叠顺序改为一个隔离智能体真正发问的顺序；所有权移到最后并重新框为推论 | §8 | 编辑性 |
| 「没有命令」改以*没有投递语义*论证，而不是*寻址无法表达* | §1.3 | 规范性澄清 |
| 规范化改用 **RFC 8785（JCS）**；删去仅针对 `ts` 的尾随 `.0` 规则 | §5.9 | **破坏性** |
| `refs` 的值：空字符串与 `null` 改为在追加时拒绝，而不是在哈希时静默丢弃 | §5.4、§10.2 | **破坏性** |
| `current(S)` 简化：后继 MUST 携带 subject；删去前向游走；保留命名空间的事实不是寄存器成员 | §8.1 | **破坏性** |
| `supersededBy(F)` 定义为**直接**后继，并给出 tie-break；排除被撤回的与未授权的后继 | §8.1 | **破坏性** |
| 一条事实 MUST NOT 携带多于一个生命周期 ref；总线拒绝之 | §5.4、§8.4、§10.2 | **破坏性** |
| `resolves` 门控收紧到**仅当前领取胜出者** —— 删去未门控的「从未被领取」路径 | §8.4 | **破坏性** |
| Δ 是日志的属性，由总线发布；读者 MUST NOT 覆盖它 | §8.4、§B | **破坏性** |
| `trust` 新增 `retracted` 状态；`quorum` MUST ≥ 1；无法识别的 verdict 不再占住其作者的名额 | §8.3 | **破坏性** |
| 折叠定义在**完整前缀**之上；被过滤/部分的窗口是非规范性的 | §8.0 | 规范性澄清 |
| 悬空祖先以显式缺口标记呈现，而不是静默截断 | §8.2 | **破坏性** |
| `supersedes` 现在以目标的作者门控，堵住一个信任劫持 | §5.4、§8.1、§10.1 | **破坏性** |
| `tombstones` 现在以目标的作者门控，堵住一个数据销毁原语 | §8.1、§10.1、§11.2 | **破坏性** |
| 新增：授权模型 —— 哪些 `refs` 键是自断言的、哪些需折叠门控 | §10.1 | 新增 |
| 新增：每个字段的值域与硬上限 | §5、§B | 新增 |
| 新增：互斥性、确定性、单调性、幂等性的定理与证明 | §9 | 新增 |
| 新增：一致性层级，以及一套向量集必须钉住什么 | §A | 新增 |
| 恢复 MUST 截断撕裂的尾部；`seq` MUST NOT 被复用 | §11.1 | **破坏性** |
| 压缩 MUST NOT 改变任何折叠结果；「被取代」不再单独构成丢弃 payload 的依据；被剥过的事实 MUST 被标记 | §11.2 | **破坏性** |

### C.3 扩展策略

- 新增一个 `refs` 键是**可加的**：总线 MUST 接受未知键，读者 MUST 忽略自己不理解的键
  （M13）。一个需要折叠门控的新键**不是**可加的，需要一次版本变更。
- 在保留命名空间之外新增一个事实 `type`（§5.1）永远是可加的。
- 新增一个字段**不是**可加的：未知顶层字段不被存储（M14）。扩展放进 `payload`。
- 任何对内容地址、对某条 §8 折叠、或对某个 §10.1 门控的改动，都是破坏 wire 的，需要一个大版
  本以及一套重新生成的向量集（§A.2）。

---

<div align="center">
  <sub>

*协议 v3.0 由 Carter.Yang 设计。总线负责定序与保存；含义由读者决定。*

  </sub>
</div>
