# Node
node_modules/
# The dsh launcher setup-dcu-profile.sh installs locally, so a checkout can
# boot a DCU without a global dsh. It is a node_modules tree by another name.
.dsh-launcher/
dist/
build/
*.tsbuildinfo
.npm/
.eslintcache

# Runtime state — never commit. The bus journal (ANTLEGION_DATA_DIR, default
# .data-v2/) and the colony residency dir (.ant/ — pid, logs, prompts, and the
# agent's cross-wake working memory) are produced wherever you run the tools,
# so match them at any depth, not just at the repo root.
.data-v2/
.data/
.ant/
*.jsonl
*.jsonl.tmp
*.log

# Environment
.env
.env.local
.env.*.local

# Internal research notes (not for the public repo)
research/oa-*.md
research/dcu-board-visualization-*.md

# IDE
.claude/
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Cowork planning docs (local only)
.cowork/

# verify-cli-eventflow self-booted bus data
deploy/.verify-data-*/

# Local dev overlay — carries an absolute path; copy from the .example.
dsh-antlegion/cordis.patch.local.yml
